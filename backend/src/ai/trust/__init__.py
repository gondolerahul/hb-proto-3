"""ai.trust — Increment 2 TRUST: billing-safety, consent & the economics floor.

Ships in slices. First landed: the **consent registry (D6)** — the
jurisdiction-agnostic consent/DNC/unsubscribe store + the tenant-managed
provider that fills the KAR ``solo_pack/consent.py`` seam via
``install_consent_registry``. Dunning/degraded (C5), checkpoint SLAs (C3),
platform-spend class (B13), and the economics findings (E1/E2/E4) follow.
"""
