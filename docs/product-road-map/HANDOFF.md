# Product Road-Map — Development Handoff

> **Purpose:** resume development in a fresh session with full context.
> **Last updated:** 2026-07-29 · **Author:** Claude (session with Rahul)
> **Read next:** [build_roadmap.md](./build_roadmap.md) (the plan) · [roadmap_gap_register.md](./roadmap_gap_register.md) (findings) · the `increment-1/` and `increment-2/` folders (per-workstream design + build notes).

---

## 0. TL;DR — where we are

* **Increment 1 (One-Loop Foundations) — ✅ BUILT & MERGED to `master`.** Four workstreams: **SIG** (signal bus), **GOV** (governance + PolicyGate), **SCH** (tenant schema / records / data plane), **LOOP+ENV** (Loop runtime, budget envelopes, wallet holds). Register findings B1/B2/B3/B5/B6/B8/E3/A6 closed.
* **Increment 2 (The Solo Pack — the sellable MVP) — DESIGNED + SLICE, PACK, KAR built; ONBOARD backend built.**
  * All 6 workstream design docs written and decisions locked (`increment-2/`).
  * **SLICE** (email→quote vertical slice) — ✅ BUILT & **MERGED to `master`** (and on the remote). Closes C1 for P03.
  * **PACK** (full Wave-0 roster) — ✅ BUILT & **merged to local `master`** (push pending, §1). 16 entities + 7 bundles + generalized activation + runtime SoD. Closes C1 (all 6) + Inc-1 owner-id/governance carryovers.
  * **KAR** (outward gateways) — ✅ BUILT & **merged to local `master`**. KAR-03 WhatsApp (via SIG) + KAR-01 voice stub; roster **18**. Karuna threat posture + consent seam. Real voice/B7 → Inc 3.
  * **ONBOARD** (setup wizard) — ✅ **COMPLETE: backend + frontend.** The wizard **step APIs** (`/ai/onboarding/*`, Pragya's Inc-3 stages) + the budget-envelope admin GET (2026-07-20); the **React surfaces** (Solo Pack wizard rebuilt over the step APIs, `/admin/loop-ops` signals/triggers/envelope page, checkpoint-aware approvals console + the `/respond` body-contract fix) built 2026-07-22 on `inc2/onboard-fe` — see [04](./increment-2/04_onboard_wizard.md) §7.
  * **TRUST** (billing safety + consent) — ✅ **COMPLETE.** C5 + D6 + C3 + B13 (migrations `trust001`–`trust004`); **E1 + E2 + E4 + both integration hookups** built 2026-07-22. All 7 findings closed.
  * **RETR** (retrieval upgrade) — ✅ **COMPLETE.** All 5 tasks on branch **`inc2/retr`** (2026-07-22, not yet merged): hybrid+RRF, structure-aware chunking + lazy re-chunk, schema-aware filters + KB domain viewport, Growth+ LLM rerank, retrieval goldens. Migrations `retr001`–`retr003`. Closes B8's retrieval half.
  * **Increment 2 is COMPLETE** — every charter finding closed and the ONBOARD frontend built (2026-07-22). Nothing Inc-2 remains but the push (§1).
* **Increments 3, 4, 5 — ✅ BUILT & MERGED to local `master`** (per-workstream map in §2–§3): Pragya v1 + AUTH + VOICE · PRAGYA-RT + CONN/SOR · REG/RTR/EVX/FLEET.
* **GENUI Design Gate — ✅ PASSED at design level (2026-07-24):** ten-concept brainstorm + owner selection + the full **Vihara** spec ([genui_design_gate_spec.md](./genui_design_gate_spec.md) v1.1, ratified). **Next: Increment 6** (LEARN → SEGA → GENUI) — see §3.

* **GENUI backend gap analysis — ✅ WRITTEN (2026-07-24):** the ratified Vihara spec walked against the shipped code — **23 backend gaps + 12 road-map gaps** in [increment-6/00a_genui_backend_gap_analysis.md](./increment-6/00a_genui_backend_gap_analysis.md). Headline: the rendering substrate is 100% absent (expected, that's G0), but so are three *net-new subsystems* the road map never scoped — the **Glasshouse**, the **strategy pipeline**, and **push/Private-Line** infrastructure. Six owner questions open there (§6); see §3 below.

* **Increment 6 (The Self-Improving Platform) — ✅ COMPLETE (2026-07-26).** Nine decisions locked; **all six workstream docs written** (LEARN · SEGA · TWIN · STRAT · GATE · LIB — see §3); VG-05's backend gate *and* its console half both built and merged. **All six workstreams are built and merged** — LEARN · SEGA · TWIN · STRAT · GATE · LIB. B10, B11, D3 closed, plus gap-analysis VG-09/10/11/12/13/14/15/16/17; migration head **`lib002`**. With B11 and D3 shut, **every High-severity finding in the gap register is closed.** The design round surfaced **two live defects in shipped code**, both now fixed: unscoped tool-registry reads (cross-tenant disclosure, SEGA T0) and 64 uncategorised social/ad tools (ungoverned public broadcast + ad spend, GATE T1–T2). STRAT's object sheets got the treatment the HBS spine and the Pragya stage scripts got — drafted, reviewed by Rahul (2026-07-26, eleven questions), then built.

* **Voice go-live (VG-08) — ✅ BUILT 2026-07-28, awaiting a live call.** Five commits after the Increment-6 close, undocumented here until now. **All six gaps closed** — including two nobody knew about: **gap 5**, no Pragya entity ever existed (`PRAGYA_ENTITY_NAMES` was only ever *read*, from Increment 3 until a phone number needed somewhere to point), and **gap 6**, barge-in was silently dead because it keyed off ASR interim results and Chirp 3 emits none. Both live transports (Chirp 3 over Speech-v2, Gemini TTS over Vertex) are **written and never executed**. Migration **`iauth002`** (one-address-one-tenant, partial unique index). What remains is a phone call, blocked on two config items: the ASR row's region reads `us-central1` and Chirp needs `us`, and ADC needs re-authenticating on this VM. Resume at [increment-7/00a](./increment-7/00a_voice_go_live_plan.md) §8.

* **Increment 7 (Vihara) — ✅ PHASE A EXITED 2026-07-29; Phase B under way, SEAM BUILT.** The clarifying round ran (**eight decisions locked**, [increment-7/00_charter.md](./increment-7/00_charter.md) §Decisions), **all eight Phase-A deliverables are written** ([01_phase_a_overview.md](./increment-7/01_phase_a_overview.md)), and **both blocking reviews passed 2026-07-29**: **R1** (art bible — all eight §12 rulings approved; **portraits are direction A with the procedural seal as fallback**, VP-02 resolved) and **R2** (wireframes — all seventeen surfaces as drawn). **VP-03 resolved the same session: The Study** ([09](./increment-7/09_screen_parity_register.md) §4). The **workstream decomposition is written** ([10_workstream_decomposition.md](./increment-7/10_workstream_decomposition.md)): **SEAM → SUB → WORLD → DRIVER → STEWARD**, LINE parallel after SUB, GLASS after DRIVER, POLISH last. **SEAM is BUILT (2026-07-29, `inc7/seam`, T1–T10, merged):** the `ai/genui/` package — registry serving (48 authored entries in `vihara/src/manifest/registry/`, R5 correspondence test), estate read model (VG-02), manifest service (VG-01 — **manifests are pure shapes**, cache cross-tenant-safe by construction), SSE stream (VG-03), tray composer (VG-04), echo bus (VG-06), push + single-writer sender (VG-19), Pragya's WS channel (VG-07), and **VP-01** (cookie-mode token delivery). Migration head **`genui001`**; two owner decisions landed mid-build (**port 4044**; push verified). Build notes + nine deltas: [06](./increment-7/06_backend_api_contracts.md) §12. **SUB is BUILT too (2026-07-29, `inc7/sub`, T1–T8, merged) — G0's exit is met at the test level:** the `vihara/` app on port 4044 — tokens mirrored from the brand with a sync gate, the client manifest layer with the D4 §7 refusal ladder, the memory-only-token API client (VP-01's consumer, storage-pinned), the Sheet/Card region engine with the v1 components, **the ten certified components golden-rendered** (40 structural goldens, the cross-context assertion, two mutation refusals each), pre-session screens, and the Still Surface round trip — **proven cross-language** by captured NDJSON wire fixtures gate-tested on both sides. Vitest **117** · shell **84.7 KB gz** of the 220 hard budget · backend **2132 unit**. Build notes: [02](./increment-7/02_stack_and_repo.md) §8. **WORLD is BUILT (2026-07-29, `inc7/world`, W1–W8, merged):** the pure deterministic territory layout (L4 as a tested property), the tier probe with the `surface.*` override, the world renderer with the §13 construction language (plinths, ghost scaffolds, surface-printed roads, energy floor light-never-gold, the beacon as the only repeating attention animation), offered-not-imposed demotion + the context-loss switch, the SSE stream client with an idempotent reducer, the procedural seals, and the depth ladder still → terrace → district. **The bundle proof: shell 87.3 KB gz, world chunk 215.5 KB gz, tier C provably never fetches three.js.** Vitest **138**. Build notes: [08](./increment-7/08_device_matrix.md) §9. **The A-direction portraits are BUILT (2026-07-29, `inc7/portraits`, merged)** — art bible §7.1a: Imagen 4 via ADC, ten owner-approved personas, two style rounds, **the owner picked T4 (luminous)**; ten busts promoted as pure SVG dot geometry (test-pinned: gold ramp + ground only, >500 dots each, frozen once published) with the `Portrait` component falling back to the seal. Charter decision 8's pre-G1 obligation is **closed**; **G1's exit keeps two owner-side items** (§9.3: the real-device matrix run · the walkable look). **DRIVER is BUILT (2026-07-29, `inc7/driver`, D0–D13, merged) — G2's exit met at the test level.** Three owner decisions locked at the session start ([11_driver.md](./increment-7/11_driver.md) §2): the Study draft-and-build · the cost estimator designed **and built** · VG-18 termination a **plain governed act** (the safe direction — certified set stays ten). What shipped: the **Tray** (HITLPanel's replacement — the four D6 §4 rules pinned by test, `useCertifiedAct` + the WebAuthn/TOTP ceremony, every path echoing), the **per-path cost estimator** (`genui/cost.py` — observed medians, five-observation floor, never summed with the gate's own amount), **Registry Halls** (schema-derived CRUD; **bulk became the seventh gated call site** via `POST /ai/tenant/records/bulk` + R5's new `CEREMONY_ONLY_GATES` exception family), the furnished **district sheet + dossier**, the **Standup**, the **Boardroom** (STRAT's Planning records finally get their producer; UNTESTED ≠ UNKNOWN), the **Talent Office + VG-18** (new strict package `ai/talent/` — termination refuses over live runs, files a real handover-memo artifact, stamps the Gallery record; **no migration**), the **Gallery**, the **Library** (influence binds `distinct_queries`), the **Undercroft + manifest inspector**, **Bridges & Gates**, and the **Study** (drafted into D6 §15a first, then built). Build notes + six deltas + honest limits: [11_driver.md](./increment-7/11_driver.md) §6. **STEWARD is BUILT (2026-07-29, `inc7/steward`, S0–S10, merged) — G3's exit met at the test level; the live legs stay owner-side.** Four owner decisions locked at session start ([12_steward.md](./increment-7/12_steward.md) §2): voice now as a tested seam · the tray recommendation **LLM-written** · platform voice stays parked · STEWARD only. What shipped: the **approval watcher** (the production `deliver_tray` caller SEAM honestly lacked — an API-process lifespan task because the socket hub is in-memory there; the ledger's (approval, user) grain does once-only *and* late-arrival; migration **`genui002`**), the **recommendation writer** (`TRAY_RECOMMENDATION`, tenant-side of B13 by a named test, written once at first delivery, failure → null never a blocked tray), **navigation** (a read materializes, an act focuses, at most one event per turn; the narration now speaks **to the session** — rule 1 beats the reply shape; a failed turn is presence `away`, never a dead channel), the **browser voice leg** (the same Chirp/Gemini adapters with μ-law conversions off; voice is an *input method*, not a channel — console tier rules, **no T1 ceiling**, AST-pinned; the defect the tests found: a mic close that awaited the drive made barge-in structurally impossible), and the **client dock** (`vihara/src/steward/` — reconnecting channel client, presence mark, narration with anchors, ceremonies through the **same** `StepUpCeremony` retried whole once, **the T3 second-channel leg driven end to end**, PCM16 mic + client VAD + the butt-joining player, viewport on every depth change, and the one-SSE-per-app consolidation DRIVER promised). Certified set **still ten**. Build notes + seven deltas: [12](./increment-7/12_steward.md) §9. **LINE is BUILT (2026-07-29, `inc7/line`, L0–L10, merged) — G4's exit met at the test level; the real-device legs stay owner-side.** Three owner decisions locked ([13_line.md](./increment-7/13_line.md) §2): the WhatsApp mirror is the one door's **last resort** (socket → push → WhatsApp → nowhere) · the Morning Story ships with **pre-generated daily audio** (the ambitious option — its obligations, a store + a cost class + text-degrade, owned in §5 there) · the **daily WhatsApp morning summary** ships now. What shipped: **VG-20 closed, and smaller than sketched** — the story is the Standup's composition *ported and shared* (`genui/morning.py`), the thread already persisted (`pragya_turns`), the passkey IS the device registration; the one new table is `morning_stories` (**`genui003`**, audio-on-row, 30-day reap in the producing job). The **morning job** (02:25 UTC: compose · WAV-wrapped TTS through the shipped adapters · store · WA summary; `MORNING_STORY` classified tenant-side of B13 by a named test; wallet gates synthesis; every failure degrades to text with its reason named), the **mirror** (verified-binding-or-nothing, two single-writer import boundaries; the delta worth reading: the India-first WhatsApp-only user was in **no recipient set** until `_whatsapp_users` joined the sweep — the leg built for that case would never have fired), and the **pocket app** (`line.html` second entry at its own hard 220 budget — measured **3.5 KB gz**, shared chunks ride the cached shell; the service worker refuses background sync and offline queues — an offline approval would be a certified act with no server; the Thread's certified section **is TraySurface**, so the biometric bar is the platform passkey unchanged; the Morning Story swipes with echoes; the Pocket Desk pins to `surface.line_pins`; the iOS install-first ceiling is *stated*). Build notes + five deltas: [13](./increment-7/13_line.md) §8. **GLASS is BUILT (2026-07-29, `inc7/glass`, X0–X6, merged) — G5's exit met at the test level; the pilot walkthrough stays owner-side.** Four owner decisions locked ([14_glass.md](./increment-7/14_glass.md) §2): the twin binding is **context-scoped** · the runner is an **arq job** · the promotion chain goes **all the way to SEGA** · a **wallet hold** is drawn before the run. The assessment finding that shaped everything: **TWIN's substituted registry had no consumer and could not have had one** — the executor resolves tools from a class-level global, the record tool defaults to `Plane.LIVE`, and `agent_loop.py` is line-capped, so a twin run assembled from TWIN's pieces alone would have executed *real* tools against *reality*. What shipped: the **binding seam** (`twin/binding.py` — one frozen binding on a contextvar, read at both executor sites and inside `TenantDataPlane.session` where **the plane is forced to TWIN even when the caller asks for LIVE**; mutation-tested both directions, and **`tests/parity` stayed 16 green** across a change to a path every live run takes), the **scenario runner** (`twin/runner.py` — the assembly TWIN declared unbuilt: price → cap → wallet → materialise → substitute → replay through a handler that runs the **shipped** loop inside the binding → grade → write; a leaked external effect is a *failed* run, not a tidy result), the **arq job** + `POST /ai/twin/scenarios/{id}/run`, the **promotion chain** (`twin/promotion_chain.py` — the consumer `twin.promotion_proposed` never had, a HITL card, and a **sweep** applying through SEGA's `apply_proposal` where B11's limits bite), and the **Glasshouse room** (the surface desaturates, never the components — so a component cannot *fail* to; the ribbon is the one saturated thing; four grades with `untested` ≠ `unknown`; levers price before they are pulled). **No migration, no 22nd checkpoint, no eleventh certified act** — the certified set is still **ten**. Build notes + five deltas: [14](./increment-7/14_glass.md) §9. **POLISH is BUILT (2026-07-29, `inc7/polish`, P0–P12, merged) — G6's buildable half met at the test level; the increment's build phase is COMPLETE.** Four owner decisions locked at session start ([15_polish.md](./increment-7/15_polish.md) §2): the background palette is the **brand re-key** (light, never gold, the legacy blue stays retired) · the engine is the **full three.js legacy port on tier A/B** with the Canvas-2D floor as tier C's version and the fallback · the **gold budget stands** (luxury through material, light, motion and type — never more gold) · **fonts self-hosted** (the Google @import left the canonical DS file; latin+latin-ext carries ₹). The session opened with the owner's review: *no screen was close to the R2 wireframes* — and the diagnosis held structural: the wireframes' background system (the legacy energy floor, ported and re-keyed, annotated as such in `still-visual.html`) was never built, the DS's material layer (`styles.css` — liquid glass, semantic type) was consumed nowhere, the §9 motion language was unimplemented, and three late workstreams had drifted onto undefined `--vh-*` variables. What shipped: **P1** the foundation (drift dead by test · `styles.css` mirrored under the two-copies gate · self-hosted fonts) · **P2/P3** the **atmosphere layer** (vignette + Canvas-2D energy floor + dotted-B watermark + day–night luminance at every depth, and the legacy three.js scene ported whole — bloom, pointer ripple — living in `renderers/world/` because only that tree may import three.js, GL **shell-only** by tested rule, falling back to 2D on context-loss ×2 / FPS breach / no WebGL, never a black rectangle) · **P4** glass per the art bible §10 placement map with a correspondence test (every backdrop-filter selector appears in the reduced-transparency fallback; glass never inside a sheet) · **P5** the §9 motion table (the beacon's ~4s breath the only `infinite` in the CSS, by test; full glow kept under reduced motion — the one element the DS's blanket collapse gets wrong) · **P6** the shared-idiom surface pass (interaction grammar, still-visual composition at depth 0, mono eyebrows, metallic certified seal) · **P7** onboarding staged in the world (**depth 0 unreachable before stage 9**, failing open by test — a sequencing rule, not a security control) · **P8** the §11 contrast table **computed from the mirrored tokens** + axe over the wire-free surfaces · **P9** VG-22's harness half (p75 render canary: still 10.5ms/120 · terrace sheet 16.5/200 · tray 10.2/250) · **P10** the parity reckoning (register v1.3 — 28/28 replaced rows live, **two named residuals**: password reset, login OAuth) · **P11** [15a_launch_protocols.md](./increment-7/15a_launch_protocols.md), the one document holding **every** owner-side leg (the §10.4 five-user script, the 30-day parallel-run checklist, the one-sitting device run sheet). **No backend change, no migration.** Build notes + six deltas + honest limits: [15_polish.md](./increment-7/15_polish.md) §9. Background: the **Buddha Cognitive Lab design system** landed that week, and charter decision 2 made it canonical across all three renderers — **superseding ratified spec §11**.

**Everything committed is green** (re-measured 2026-07-29 after the POLISH merge): mypy `--strict` **345 files**, layout lint, **2257 unit+parity/eval** (+3 skipped) — POLISH touched no backend source, migration head **`genui003`** unchanged. Vihara: **288 vitest** (+41), bundle gate **shell 120.4 · line 3.5 · world 218.9 KB gz** (the atmosphere added ~1.8 to the shell; the bloom passes joined the world chunk), `tsc` + eslint clean, `axe-core` joined devDependencies, **fonts now self-hosted** (the canonical DS file edited by owner decision — six variable woff2s under both mirrors). **`origin/master` is now ~70 commits behind** (SUB's tail, WORLD, portraits, DRIVER, the vhost deploy, STEWARD, LINE, GLASS and POLISH are local-only) — this VM still has no git credentials, so **a push from a credentialed host is the standing owner-side action**. **Voice go-live's two config items are done** (ASR row `region=us`, ADC authenticating) — the live-call proof remains, on the production side; it gates **G3's exit** (with a live browser mic run), and **G4's exit** adds the real-device legs (Android + iPhone install, a push arriving as a tray, a fingerprint approving a payment).

> **Gate-count corrections (2026-07-24, still worth knowing):** the "1194 unit + 183" line was stale from Increment 2, and where this file quotes **232** integration tests that is the narrower `tests/integration/test_*_db.py` selector — the whole `tests/integration` directory collects and passes more (**490** as of 2026-07-26).

---

## 1. ⚠️ The push is BEHIND AGAIN (measured 2026-07-28: `origin/master` is **49 commits** behind)

**As of 2026-07-28, `git rev-list --left-right --count origin/master...master` returns `0 49`.** Increment 6, the voice go-live work and Increment-7 Phase A are all local-only. The section below is the 2026-07-24 history; the *capability* is proven (it was published once from a credentialed host), so this is a task rather than a blocker.

> **2026-07-24 (history):** `git ls-remote origin master` returned **`a403cda`**, byte-identical to local `master`. The long-standing "this VM has no write credentials" action is **closed**.

Re-verify at any time with:
```bash
git rev-list --left-right --count origin/master...master
```

> **Toolchain correction (2026-07-22):** earlier sessions recorded "Node/browser toolchain out of scope on this VM" — that is **stale**. Node v20.20.0 + npm 10.8.2 are installed and `frontend/node_modules` is populated, so the **ONBOARD frontend track builds here**. Note `cd frontend && npm run build` failed with ~22 pre-existing TS errors (bitrot in `pages/ai/*` + `useAuth` + `IntegrationsPage`, unrelated to ONBOARD) — fixed as task 0 of the FE track.

---

## 2. What's built (map)

### Increment 1 (on `master`)
| Workstream | New package(s) | Migration | Key tables/seams |
|---|---|---|---|
| SIG | `backend/src/ai/signals/` | `sig001` | `signals` (outbox), `trigger_registry`; dispatcher + sweeper crons; email-ingest producer |
| GOV | `backend/src/ai/governance/` (extended) | `gov001` | typed `Governance` schema, `hitl_checkpoint_defs` (18), `human_approvals.checkpoint_key`, the **PolicyGate** (a loop stage before the Pre-Critic), deploy validators |
| SCH | `backend/src/ai/tenant_schema/` | *(none — tenant tables are bootstrapped per-tenant, not control-plane Alembic)* | HBS 27-object spine, record service (CAS + owner-writes/others-propose), two-backend data plane (schema / container), memory viewport, export |
| LOOP+ENV | `backend/src/ai/loop/` | `loop001`, `loop002` (Sheel seed) | `loop_runtime` + heartbeat/watchdog crons, `budget_envelopes` (protected reserve), `wallet_holds` (closes the E3 race), `credit_wallets.wallet_debt` |

### Increment 2 / SLICE (merged to `master`)
* `backend/src/ai/solo_pack/` — `templates/` (curated entities), `loader.py` (GOV-validating), `activation.py` (seed + owner-id resolve + triggers), `tools.py` (`tenant_record_write`, `emit_business_signal`).
* GOV `authority.py` gained the **`email_dispatch`** category (so an A1 `send_email` raises a HITL card).
* The **`test_solo_pack_slice_e2e.py`** is the north-star test — the sellable path email→approved-quote across real SIG+SCH+GOV seams.

### Increment 2 / PACK (on `inc2/pack`)
* `solo_pack/templates/` split per domain — `acquisition.py` (SLICE 4), `care.py` (P06 + AGT-030/035/092), `finance.py` (P08+AGT-038 maker, P10+AGT-046 checker), `compliance.py` (P14+AGT-068 auditor), `intelligence.py` (P19+AGT-051); `__init__` is the aggregator + `PROCESS_GROUPS` manifest. **16 entities total.**
* `solo_pack/bundles.py` — the 7 §2.1 starter bundles (numeric process codes → dodge the de-canary lint). `activation.py` generalized: `activate_solo_pack` / `activate_bundle` / `activate_slice` over one `_activate` core.
* GOV: `schemas/capabilities.py` gained `sod_tags` (persists the §9.4 conflict tags); `solo_pack/tools.py` resolves the acting process from `agent_id` → cross-owner writes **propose** (runtime SoD).
* Docs: `increment-2/03a_wave0_process_sheets.md` (5 sheets, C1), `03b_pack_behavioral_goldens.md` (per-agent contracts). Tests: extended `test_solo_pack_templates.py` (57), `test_solo_pack_activation_db.py` (+pack roster), new `test_solo_pack_sod_db.py` (maker/checker holds).

### Increment 2 / KAR (on `inc2/kar`)
* `solo_pack/templates/gateways.py` — a shared `_karuna_gateway` builder + **KAR-03 WhatsApp** (consumes `message.inbound`) + **KAR-01 voice stub** (parks `voice.*`). Added to `GATEWAYS` → roster is **18**; `activate_slice` seeds email-only.
* `signals/whatsapp_inbound.py` — the WhatsApp producer (`message.inbound`, `trust:counterparty`, SID dedupe, subscription-gated). Wired into `src/voice/whatsapp_handler.py` as a fail-safe cutover. New `SignalTypes.MESSAGE_INBOUND`/`VOICE_INBOUND`.
* `solo_pack/consent.py` — the outbound consent/DNC seam (`check_outbound_consent` + pluggable checker; TRUST installs D6). Tests: `test_kar_gateways.py` (injection golden via pure `evaluate_policy` + consent), `test_whatsapp_inbound_db.py` (emit/dedupe/subscription/dispatch→KAR-03), gateway-posture in `test_solo_pack_templates.py`.

### Increment 2 / ONBOARD backend (on `inc2/onboard`)
* `solo_pack/onboarding.py` — the wizard steps as functions (Pragya's Inc-3 stage contract): `list_bundles`, `governance_preview` (pure), `activate_for_company`, `onboarding_status`. `solo_pack/onboarding_router.py` — `/ai/onboarding/{bundles,governance-preview,activate,status}`, company-scoped. `loop/api.py` — `GET /ai/loop/envelope` (the envelope admin view). Both registered in `src/main.py`.
* Admin backends: signals inspector + trigger editor were already shipped (`signals/api.py`); ONBOARD added only the envelope GET. Tests: `test_onboarding.py` (pure steps), `test_onboarding_db.py` (activate + status + envelope). **Frontend not built** (separate track).

### Increment 2 / TRUST — D6 consent registry (on `inc2/trust`)
* New package `ai/trust/` (added to the strict allowlist in `scripts/typecheck_ai.py`): `models.py` (`consent_records`/`dnc_entries`/`unsubscribe_log`), `consent_registry.py` (`evaluate_consent` + `TenantManagedProvider` + `install_consent_registry`). Migration **`trust001`** (off `loop002`).
* Wired into KAR's `solo_pack/consent.py` seam via `install_consent_registry()` at `main.py` + `worker.py` boot (beside `register_solo_pack_tools`). Tests: `test_consent_registry.py`, `test_consent_registry_db.py`.
* **C3 per-checkpoint HITL SLAs:** `governance/checkpoints.py` (per-category SLA policy + stamps the 18 seed rows), `governance/models.py` (`sla_seconds`+`on_timeout`), `governance/checkpoint_sla.py` (`apply_checkpoint_timeouts` sweep), wired into the 60s `signal_sweeper` cron. Migration `trust002`. Tests: `test_checkpoint_sla{,_db}.py`.
* **B13 platform-initiated budget class:** `cost_attribution.py` (`PLATFORM_INITIATED_ATTRIBUTIONS`), `loop/models.py` (`budget_class`), `loop/envelopes.py` (tenant rollup excludes platform attributions; class-aware), `loop/platform_budget.py` (`platform_spend_admitted` — parks platform work at its own cap). Migration `trust003`.
* **C5 graduated dunning + state-aware suspension:** `ai/trust/dunning.py` (the `current→…→suspended` ladder, `advance_dunning`, `agents_may_act`), `companies.subscription_status` (migration `trust004`), `common/middleware.py` (`CompanySuspensionMiddleware` now degrades to read-only — blocks agent mutations, allows GET/billing/export — before hard suspend). `BILLING_GRACE_DAYS`/`BILLING_READ_ONLY_DAYS` settings. Tests: `test_dunning{,_db}.py`. **Remaining TRUST (E1/E2/E4) not built** — economics only; see [05](./increment-2/05_trust_billing_safety.md) §11.

### Increment 2 / TRUST — the economics floor (on `inc2/trust-econ`)
* **E4** `ai/trust/fee_guard.py` — the shipped code had **no** `max(total_billing, 0)` clamp at all (worse than the register assumed: a negative TB *credited* the customer). The guard clamps *and* alerts (ERROR + `billing.fee_misconfigured` signal naming the culprit, deduped per company/period), wired into `compute_billed_amount` + `record_billing_event`. Ordering intent (discount subtracted last, never reducing the partner fee) is asserted and pinned.
* **E2** `ai/trust/abuse_controls.py` — verification gate + per-IP signup throttle (stamped into `onboarding_metadata`, **no new table, no Redis**) + eligibility predicate. Enforced at **all three** credit-injection points (`_provision_new_tenant`, `get_or_create_wallet`, `flush_and_inject_daily_credits`) because any one alone is bypassable. Settings `TRUST_REQUIRE_VERIFIED_FOR_CREDITS`, `TRUST_SIGNUP_MAX_PER_IP_PER_DAY`.
* **E1** `ai/trust/idle_cost.py` + [`increment-2/05a_idle_cost_model.md`](./increment-2/05a_idle_cost_model.md) — structure derived from shipped cron cadences, rates declared. **$0.30/tenant/mo** Solo, $4.44 Growth; the Blueprint's $2,000 is platform *fixed* cost, mis-scoped. **Key result: free credits are $150/mo/tenant vs a $0.30 idle floor (500×) — the free tier's risk is entirely the credit grant, which makes E2 the GA-gating economics item.**
* **Hookups** — `billing/cron_service.days_past_due()` + `run_dunning_job()` + `ai/trust/crons.py::dunning_sweep` (arq daily 01:10 UTC, plus `POST /api/v1/cron/dunning`) drive C5's ladder; `loop/platform_budget.platform_work_admitted()` wired into `dreaming_worker` closes B13's runner gap.

### Increment 2 / RETR — the retrieval upgrade (on `inc2/retr`)
* **T1** `memory/hybrid_retrieval.py` — lexical (Postgres FTS, GIN index `retr001`) + semantic (pgvector) fused by **reciprocal-rank fusion**. Fusing on *rank* is the design: a cosine and a `ts_rank_cd` are incomparable quantities. `search_semantic` now degrades to lexical-only on an embedding failure instead of returning nothing.
* **T2** `memory/chunking.py` — structure-aware chunks (ATX/numbered/setext/bare-title headings, per-source-type sizing) replacing `text[i:i+500]`. **Heading context is prepended into `content`** because both retrievers read that column. `memory/rechunk.py` + hourly cron is the lazy upgrade (`chunk_version`, migration `retr002`), gated by B13's platform budget since re-embedding is platform-initiated.
* **T3** `memory/retrieval_filters.py` + `documents.memory_domain` (`retr003`) — relevance predicates (bound, never interpolated) and the Inc-1 **domain viewport now applied to the KB**, after fusion and before `top_k` so an invisible document can't eat a result slot.
* **T4** `memory/reranker.py` — Growth+ **LLM reranker** (decision resolved 2026-07-22). Tier gate fails closed, top-20 window, attributed `rerank` and deliberately *tenant*-initiated, fails open on every path.
* **T5** `tests/eval/retrieval_{metrics,corpus}.py` + `test_retrieval_goldens_db.py` — the regression gate. **MRR 0.812 → 1.000** vs cosine; all the gain on exact-token queries, paraphrase queries unregressed.

### Increment 3 / VOICE — realtime voice in the governed loop (merged to `master`)
* `backend/src/ai/voice_loop/`: `profile.py` (**B7's answer as data** — the PolicyGate is pure so governance stays inline; Strategize/Pre-Critic/Post-Critic/Reflect/Decide defer), `live_gate.py` (a governed act is *promised*, never completed on a turn), `identity.py` (caller ID resolves a binding but **voice can never self-elevate**; T3 unavailable on the channel), `handoff.py` (same media session, ceiling clamps downward only), `deferred.py` (post-call queue, deduped on `call_sid`), `models.py`.
* `signals/voice_inbound.py` — the `voice.inbound` producer (counterparty trust, subscription-gated, SID dedupe), wired into `src/voice/webhook_router.py` as a fail-safe cutover; end-of-call queues the deferred run.
* **KAR-01 is real** — `templates/gateways.py` replaced the Inc-2 stub; roster stays 18. The template carries the realtime profile in `metadata_extensions` so it shows up in the governance preview. Migration **`voice001`**.

### Increment 3 / PRAGYA — the account-manager engagement (merged to `master`)
* `backend/src/ai/pragya/`: `stages.py` (the pure 9-stage machine — linear except stage 9 re-enters 4–6), `models.py` + `engagement.py` (`pragya_engagements`, `pragya_turns`; artifacts merge, never replace), `intents.py` (extraction → tier; the keyword screen runs *alongside* the model and can only raise), `conversation.py` (classify → authorise → execute → generate, in that order), `commands.py` (pause/resume/demote behind `require_tier`), `deployment.py` (stages 6–9 over the unchanged Inc-2 wizard functions), `api.py` (`/ai/pragya/*`), `scripts/stage_{1..5}.py` (**reviewed assets**).
* `backend/src/ai/kpi/` (**C6**): `definitions.py` (10 KPIs — formula, prerequisites, baseline, `captured_today`), `compute.py` (the honest-absence rule), `api.py` (`/ai/kpi/{definitions,business}` — one read path for Pragya *and* the dashboards).
* `governance/demotion.py` + `demotion_sweep.py` + `crons.py` (**C4**): triggers, the daily sweep (arq 01:40 UTC), `governance.autonomy_demoted`, and `promotion_evidence_sufficient` — which refuses a promotion backed by acceptance rate alone.
* Migration **`prag001`**. Frontend: `pages/ai/PragyaConsole.tsx` (+ CSS, route `/pragya`), `services/pragya.service.ts`. Renderer `backend/scripts/render_stage_scripts.py` regenerates [02a](./increment-3/02a_stage_scripts.md) from the scripts.

### Increment 3 / AUTH — the inward-channel floor (merged to `master`)
* `backend/src/ai/inward_auth/` (added to the strict allowlist): `tiers.py` (the pure T0–T3 classifier over the §20 `CATEGORY_RULES`), `sessions.py` (`elevate` / `effective_level` / `require_tier` + lockout), `step_up.py` (TOTP, slot-accurate replay refusal), `webauthn_ceremony.py` (platform-built passkeys, single-use challenges, sign-count regression rejected), `bindings.py` (OTP enrollment handshake, inbound resolution, alert fan-out), `oob.py` (both-legs-or-nothing T3), `api.py` (`/ai/authn/*`).
* Migration **`iauth001`** — six tables: `channel_bindings`, `account_manager_sessions`, `webauthn_credentials`, `webauthn_challenges`, `totp_secrets`, `oob_confirmations`. New signal types `authn.channel_otp` / `authn.oob_confirm` / `authn.security_alert`; new `INWARD_AUTH_*` + `WEBAUTHN_*` settings.
* Frontend: `services/authn.service.ts`, `components/SecuritySettings.tsx` (mounted in `UserSettings`), `components/StepUpModal.tsx`.

### Increment 4 / CONN+SOR — the connected business (on `inc4/conn-sor`)
* `backend/src/ai/connectors/`: `catalog.py` (the §6.6 manifest — MCP_SERVER / OWN_ADAPTER / PLATFORM_INTERNAL, real HBS masters, Zoho the flagship), `models.py`+`service.py`+`resolver.py`+`credentials.py` (durable per-company bindings, encrypted creds, rehydrate into the shipped MCP seam, the live-transport boundary isolated), `sync.py` (`object.synced` ingest + sweep over a `ConnectorSyncSource`), `writeback.py` (the `SorConnector` bridge + `install_connector_writeback`), `zoho_books/` (the reference adapter: `MCPClient` + `SorConnector` over an injectable transport), `sor_migration.py` (propose→confirm→apply), `router.py` (`/ai/connectors/*`).
* SOR in the record service: `tenant_schema/sor.py` (the pluggable write-back seam), `record_service.py` (write-back-first + master-wins + `sync_mirror`), `data_plane._sync_columns` (the tenant-schema additive-evolution primitive — `TenantRecord.sor`/`external_ref`), `models.py`.
* GOV: `authority.py` `external_write` category + connector-write mappings; `checkpoints.py` the 19th checkpoint (`before_external_system_write`). Migrations **`conn001`** (bindings), **`conn002`** (the 19th checkpoint backfill). New signal types `object.synced`/`sync.conflict`/`governance.sor_migrated`; `CostAttribution.CONNECTOR_SYNC` (platform-initiated, B13). Full notes: [02](./increment-4/02_conn_sor.md) §12.

**Migration head (2026-07-28): `iauth002`** — the voice go-live migration (one address belongs to at most one tenant, a partial unique index on `revoked_at IS NULL`), off `lib002`. Previously: **`lib002`** — the Inc-6 chain is `fleet001` → `learn001` → `sega001` → `sega002` → `gate001` → `lib001` → `twin001` → **`lib002`**, single head. `lib002` carries the three tables `lib001` deliberately held back (LIB T3's `document_influence_daily`, T5's `artifacts.document_id`/`record_ref`, T8's `connector_bindings.credentials_expire_at`). STRAT added **none**, as designed — its eight Planning objects are HBS seed data (spine 27 → 35). The historical detail below is left as written.

**Migration head (Inc 4, historical):** `conn002` (Inc 4 CONN+SOR, off `conn001`, off `prag002`) — `conn001` is the `connector_bindings` table, `conn002` backfills the 19th HITL checkpoint (`before_external_system_write`). The SOR mirror columns are **not** an Alembic migration — tenant tables are bootstrapped per-tenant, so they land via the idempotent column-sync in `data_plane._sync_columns` (see [increment-4/02](./increment-4/02_conn_sor.md) §12.2). Previously: `prag002` (Inc 4 PRAGYA-RT, off `voice001`; T5 added no migration — it reuses the shipped `phone_numbers` pool and the IntegrationRegistry). Previously: `voice001` (Inc 3 VOICE, off `prag001`); `prag001` (Inc 3 PRAGYA, off `iauth001`); `iauth001` (Inc 3 AUTH, off `retr003`); Inc-2 PACK/KAR/ONBOARD and the TRUST economics slice added none; TRUST's safety slice added `trust001` consent, `trust002` checkpoint-SLA, `trust003` envelope `budget_class`, `trust004` `subscription_status`; RETR added `retr001` FTS index, `retr002` chunk structure, `retr003` document domain. Run `poetry run alembic upgrade head` from `backend/` on a fresh DB.

---

## 3. How to resume — the next work

Build order (from [increment-2/00_overview.md](./increment-2/00_overview.md) §4): **SLICE ✅ → PACK ✅ + KAR ✅ → ONBOARD ✅ (backend) → TRUST ✅ → RETR ✅.** **Increment 2's backend is done.**

**Next up (decided with Rahul, 2026-07-22): the ONBOARD frontend + the Increment-3 design docs, in parallel.** The Inc-3 charter's questions round is **done** — all three answers locked (see [increment-3/00_charter.md](./increment-3/00_charter.md) §Decisions): **(1) console-first** — Pragya v1 ships as web-console chat in the shipped React app, voice/WhatsApp adapters follow (voice/B7 still fold into Inc 3); **(2) platform-built WebAuthn** — full passkey/FIDO2 in-house (+ TOTP fallback) for T2 step-up, no auth vendor; **(3) Rahul reviews the stage 1–5 scripts** — review checkpoints built into the Pragya task plan.

**Both done 2026-07-22:** the ONBOARD FE track landed ([04](./increment-2/04_onboard_wizard.md) §7 — build gate restored, wizard, `/admin/loop-ops`, approvals console + its `/respond` contract fix) and the Inc-3 design docs are written (`increment-3/{00_overview,01_auth_inward_channel,02_pragya_v1}.md`, decisions locked, task plans authored).

**Increment 3 / AUTH — ✅ BUILT & merged to local `master` (2026-07-22).** All of T1–T7: the `ai/inward_auth/` package (tier classifier, session elevation, TOTP + platform-built WebAuthn, channel bindings, T3 out-of-band leg), migration **`iauth001`**, the `/ai/authn/*` router, and the FE (`SecuritySettings.tsx`, `StepUpModal.tsx`, `authn.service.ts`). Build notes + the eight design deltas are in [01](./increment-3/01_auth_inward_channel.md) §5. **D1's mechanism is built but not yet *wired*** — no command executor calls `require_tier` because Pragya doesn't exist yet; that hookup is PRAGYA's first task and is what actually closes D1 end-to-end.

**Increment 3 / PRAGYA — ✅ BUILT & merged to local `master` (2026-07-22).** T1–T7: the `ai/pragya/` package (9-stage machine, chat transport, intent→tier extraction, command execution, stages 6–9 over the *unchanged* Inc-2 wizard functions), the **reviewed stage 1–5 scripts** (Rahul approved 2026-07-22), `ai/kpi/` (**C6**), `governance/demotion*.py` (**C4**), migration **`prag001`**, and the `PragyaConsole` React page. Build notes + nine design deltas in [02](./increment-3/02_pragya_v1.md) §7; KPI definitions in [03](./increment-3/03_kpi_definitions.md); the scripts render to [02a](./increment-3/02a_stage_scripts.md).

**D1 is now closed end-to-end** — `pragya/conversation.handle_turn` classifies every turn and executes nothing except behind `require_tier`.

**Increment 3 / VOICE — ✅ BUILT & merged to local `master` (2026-07-22).** V1–V7: `ai/voice_loop/` (realtime profile, live gate, caller identity, handoff, deferred queue), `signals/voice_inbound.py`, **KAR-01 real** (the Inc-2 stub replaced), migration **`voice001`**, and the webhook cutover. **Register B7 is closed** — see [04](./increment-3/04_voice_realtime.md), build notes §8.

**Increment 3 is COMPLETE** — AUTH, PRAGYA and VOICE all built and merged; D1, C4, C6, B7 and C8's script half all closed.

**Increment 4 / PRAGYA-RT — ✅ BUILT & merged to local `master` (2026-07-22).** Increment 3's build surfaced an architectural finding: Pragya was running on a **task** engine while her unit of work is a months-long relationship. Four gaps Inc-3 recorded turned out to be one cause. [01_pragya_runtime.md](./increment-4/01_pragya_runtime.md) locks the seam (**own turn loop; shared governance, metering, memory, tools**). **All of T1–T9 built** — the turn loop, gate boundary, artifact extraction + stage advancement, delegate/promise/report, stage reflection + the drained voice queue, behavioural goldens, child-entity delegation (decision 6), and the ASR-LLM-TTS voice face (decisions 3/5/7). Two audit-found defects fixed (the stage-2 console dead-end; the FE/API turn contract). Build notes in §12. **One honest gap: voice is a *tested seam*, not a live call** — no wire-level Whisper/Gemini call has been made; see §12.5.

**Increment 4 / CONN+SOR — ✅ BUILT & merged to local `master` (2026-07-23, merge commit `merge(inc4): CONN+SOR`).** The Connected Business: the §6.6 connector catalog + durable per-company bindings + the `external_write` governance (the 19th HITL checkpoint) + per-object mastering — write-back-first, master-wins conflicts, `object.synced` sync-in + sweep, the propose→confirm→apply ownership migration — proven on the **Zoho Books** flagship against fakes. **B4**'s mastering machine is built; **D2** and **C2** are **deferred** (per-company credentials chosen for simplicity, and the human-task surface is separable — [increment-4/02](./increment-4/02_conn_sor.md) §2, §12.3). Build notes + the six design deltas (the tenant-schema column-sync instead of a migration; the pluggable write-back provider seam; the 19th checkpoint; the two-step migration; …) are in [02](./increment-4/02_conn_sor.md) §12. **The honest limit: no live Zoho/MCP-server call is made — every external call is faked (§12.4); live binding is activation-time ops, the same discipline voice go-live carries.**

**Next up**, in priority order:
1. ~~**The push (§1)**~~ — **DONE**, verified 2026-07-24 against the live remote (`origin/master` = `a403cda` = local `master`). (Included the **CRM lead→call + voice SDK bump** WIP — pre-existing uncommitted work found in the tree at the CONN+SOR session start, preserved on `wip/crm-lead-to-call` and then **merged to `master` at the owner's request, 2026-07-23**; all gates green after the merge. Not authored this session — see the `wip:` snapshot commit for provenance.)
2. **Voice go-live** — the wire-level work Inc-4 PRAGYA-RT T5 left ([01](./increment-4/01_pragya_runtime.md) §12.5): registry rows for `pragya-asr-whisper-vertex` + `pragya-tts-gemini`, concrete Vertex/Gemini `Transcriber`/`Speaker` adapters, and the carrier-media wiring into `drive_call`.
3. **A stage-1 research executor** — `DelegationKind.RESEARCH` dispatches/promises/reports but nothing calls `web_search`; a research delegation sits `PROMISED`. Queue built, executor pending (the tools — `tools/core/{search,scraper,batch_search}.py` — exist).
4. **Increment 5 — The Intelligence Engine** — ✅ **DESIGNED (2026-07-23), build not started.** The clarifying-questions round is **done** — four decisions locked with Rahul (see [increment-5/00_overview.md](./increment-5/00_overview.md) §2): **(1)** model registry = a new control-plane `model_registry` catalog (versioned/region/effective-dated price/`capability_profile`) + per-company `IntegrationRegistry` binding; **(2)** router lives in a new `ai/intelligence/` package, `LLMRouter` delegates (no agent call-site changes); **(3)** RTR v2 complexity scoring is heuristic-first; **(4)** conservative D5 default allow-list, GLM/Qwen/Kimi opt-in with disclosure. Five design docs written: [00_overview](./increment-5/00_overview.md) + [01_model_registry](./increment-5/01_model_registry.md) (REG/B12) + [02_router](./increment-5/02_router.md) (RTR v1+v2) + [03_fleet_expansion](./increment-5/03_fleet_expansion.md) (FLEET/D5) + [04_eval_extensions](./increment-5/04_eval_extensions.md) (EVX/§22.2–.4). **Build order:** REG → RTR v1 → RTR v2 → EVX → FLEET (EVX pulled before FLEET *activation* because it is the admission gate). Two working assumptions carried from Inc-4 precedent: fleet ships as a tested seam (no live GLM/Qwen/Kimi call), and BabyBuddha stays out (now **Inc 8** after the 2026-07-24 renumber — a registry-row-shaped hole).

   **REG — ✅ BUILT (2026-07-23, branch `inc5/reg`), B12 closed.** T1–T5: the `ai/intelligence/` package (`models.py` `ModelRegistry`+`ModelPrice`, `catalog.py` the 8-model shipped fleet as declared data, `registry.py` `RegistryService`), migration **`reg001`** (off `conn002` — the new migration head), the nullable `integration_registry.model_registry_id` binding FK, `scripts/seed_model_catalog.py`. Effective-dated pricing (close+insert, never mutate) makes a past invoice reproducible; a parity-safe snapshot in `usage_service.log_usage` stamps the bound model+price without moving the charge. Gates green (typecheck 250 files, 1499 unit, 16 parity/eval, 9 REG `*_db`+unit). Build notes + four design deltas: [01](./increment-5/01_model_registry.md) §12.

   **RTR — ✅ BUILT (2026-07-23, branch `inc5/rtr`), v1 + v2.** The router in `ai/intelligence/{router,complexity,scoring,fallback}.py`, reached *through* the shipped `LLMRouter.call_llm` seam (delegates when a company's task default is `routing_mode='router'` — no agent call-site changes). **v1** reproduces the configured default (non-inferior) + writes a `routing_decisions` audit row (migration **`rtr001`**, + nullable `usage_logs.routing_decision_id`). **v2** scores the company's *credentialed, catalog-bound* models on capability-fit vs cost, downshifts as wallet headroom tightens, and falls back to the next-best model on a retryable provider fault. Non-inferiority held: **parity/eval 16 green** (the un-routed path is byte-identical). Build notes + five design deltas: [02](./increment-5/02_router.md) §12.

   **EVX — ✅ BUILT (2026-07-23, branch `inc5/rtr`).** The eval admission gate (§22.2–.4): `ai/intelligence/admission.py` (`admit_model_change` — non-inferiority within cost budget, after `require_independent_suites`) + **`RegistryService.activate`** (the load-bearing mutation-path gate — a catalog model flips to `active` *only* through it, refusing on a failed admission; router preference can't override), `ai/intelligence/canary.py` (`watch`/`roll_back`/`promote` over the routed cohort), the `MODEL_ADMISSION` B13 attribution, three signal types, `GET /ai/intelligence/admissions`, and the curated `tests/eval/routing_corpus.py`. **No migration** (signal-bus audit, platform-scoped). Admission scoring is a tested seam (ModelEval injected — the pure gate logic bites); live corpus-through-models is ops. Build notes + four deltas: [04](./increment-5/04_eval_extensions.md) §11.

   **FLEET — ✅ BUILT (2026-07-23, branch `inc5/rtr`), closes D5.** `ai/llm/openai_compat_adapter.py` (one adapter for GLM/Qwen/**Kimi** over an **injectable transport** — no live call), the three catalog rows as `preview` + `default_allowed=False`, `ai/intelligence/allow_list.py` (`effective_allow` = default-allowed ∪ opted-in, read **live**, wired into `IntelligenceRouter._enrich` so the filter runs *before* scoring), `company_provider_optin` (migration **`fleet001`**) + `/ai/intelligence/providers` opt-in/revoke, and the published [03a_data_flow_disclosure.md](./increment-5/03a_data_flow_disclosure.md). **Owner decision (2026-07-23): Kimi (Moonshot AI) replaces Mistral**, so all three expansion providers are China-hosted and opt-in — no default-allowed EU provider, a *stricter* posture than designed. Two independent gates now stand between a new provider and traffic: **tenant consent (D5)** and **EVX admission (§22.4)**. Build notes + four deltas: [03](./increment-5/03_fleet_expansion.md) §13.

   **⇒ INCREMENT 5 IS COMPLETE & MERGED to local `master`** (2026-07-24, merge commit `merge(inc5): The Intelligence Engine`) — REG + RTR (v1+v2) + EVX + FLEET all built, **B12 and D5 closed**, B9's build side done. Post-merge gates re-run green on `master`: typecheck_ai 259 files strict · layout lint · parity/eval 16 · 1523 unit · **232 integration** · migration head **`fleet001`**. Migrations `reg001`→`rtr001`→`fleet001` all apply/rollback/re-apply (new head **`fleet001`**). The honest limits, both activation-time ops: no live GLM/Qwen/Kimi call, and admission *scoring* is injected (the gate logic itself is exercised).

**GENUI Design Gate — ✅ PASSED at design level (2026-07-24).** The Inc-6 owner directive's mandatory design-and-brainstorm phase ran ahead of the increment: **ten frontend concepts** ([genui_design_gate_concepts.md](./genui_design_gate_concepts.md)), the **owner selection** (§6 there — the **Sanctum + Firm + Atlas + Twin + Private Line** hybrid, eight decisions locked incl. Sanctum-owns-depth-0, one-voice, Firm-inhabits-Atlas, simulated people permitted in the Twin, living day–night art, full-flagship build), and the **full spec [genui_design_gate_spec.md](./genui_design_gate_spec.md) v1.1** — named **Vihara** (ratified): ten Binding Laws, the depth ladder (Still Surface → Territory → Places → Undercroft), the unified estate ontology, the surface inventory replacing the 59 legacy screens, one component registry over three renderers (World/Sheet/Card), the Pragya frontend contract, T2/T3 step-up mapping, and the **G0–G6 internal gate plan**. All five §14 ratifications closed 2026-07-24 (tenant-first scope · WhatsApp read-mirror India-first, read+notify only · sound dropped · portraits in-house-samples-first). This answers Inc-6 charter open question 1; **questions 2–3 (cross-tenant learning policy B10, learning-store shape) remain for the Inc-6 clarifying round.** GENUI builds **last** within Inc 6 (LEARN → SEGA → GENUI); Vihara's G0 substrate may start in parallel.

**GENUI gap analysis — ✅ WRITTEN (2026-07-24): [increment-6/00a_genui_backend_gap_analysis.md](./increment-6/00a_genui_backend_gap_analysis.md).** The ratified spec walked law-by-law and surface-by-surface against the shipped code: **23 backend gaps (VG-01…VG-23) + 12 road-map gaps (VR-01…VR-12)**. What it changes:

* **Three net-new subsystems the road map never scoped** — the **Glasshouse** (twin plane, replay, forecast, honesty grading, scenario shelf: VG-09, no simulation code exists anywhere), the **strategy pipeline** (Minutes→Propositions→Resolutions→Mandates→Reviews: VG-11 — the HBS Planning module has exactly one object, `Budget`), and **push/Private-Line** infrastructure (VG-19/VG-20). Proposed as new **TWIN** and **STRAT** workstreams (VR-02/VR-03).
* **G5 consumes SEGA, not only LEARN/EVX** (VR-01) — the promotion pipeline's canary is SEGA's machinery with B11 blast-radius limits; `intelligence/canary.py` is model-only (VG-10).
* **Two live gaps in the shipped product, not just in Vihara:** `require_tier` has **no REST call sites** — approvals, connector credential binding, and autonomy raises are all reachable with a plain session, no step-up (VG-05/VR-12); and **voice go-live is a G3 prerequisite, not an ops remainder** (VG-08/VR-11).
* **No KPI history store** (VG-12) — the increment whose goal is "Week 12 > Week 1, *measured*" has nothing recording week 1. Proposed as an early LEARN deliverable (VR-08).
* **"GenUI replaces the React app" is false by ratification** (VR-10) — spec §14.2 keeps partner/platform-admin consoles on legacy React; cutover criteria need amending to "the **tenant** React surface".

**Increment 6 decisions — ALL NINE LOCKED with Rahul 2026-07-24** ([increment-6/00_charter.md](./increment-6/00_charter.md) §Decisions, do not re-litigate). **Nothing is blocking; there are no open owner questions on Increment 6.**

1. **B10 cross-tenant learning = SPLIT** — platform-level (routing/model performance, **no business content**) pools; tenant-level (charter tuning, density, behaviour) stays private. **Schema-level**, not a query convention: the pooled path must be *structurally incapable* of carrying tenant content.
2. **Learning store = REUSE** the signal bus + CORTEX Intelligence trees; no dedicated `learning_signals` table.
3. **Flagship = BOTH IN** — TWIN and STRAT are named workstreams; the Glasshouse ships.
4. **Certified-action step-up pulled forward** — ✅ built (below).
5. **⇒ GENUI SPLIT INTO ITS OWN INCREMENT.** Increment 6 is now **six backend workstreams**; **Increment 7 is Vihara**; **Scale & Enterprise renumbered to Increment 8**. Four reasons in [increment-7/00_charter.md](./increment-7/00_charter.md) §Why — chiefly that GENUI consumes *every other* Inc-6 workstream (LEARN's density, SEGA's canary, TWIN's Glasshouse, STRAT's mandates), so the increment could not close until its most dependent part finished. **This does not reopen the §6.8 full-flagship decision**, which was about not phasing Vihara's *launch*.
6. **Twin spend = TENANT-initiated** — stays **out** of `PLATFORM_INITIATED_ATTRIBUTIONS`, the same rule RETR's `rerank` follows. This **overrides ratified spec §12.1**, which would have let tenant experimentation exhaust the cap protecting tenants *from* platform work (B13).
7. **Pooled platform learning = disclosed, no opt-out** — it is platform telemetry, not tenant data, *if* decision 1's schema guarantee holds. The two stand or fall together. Deliberately asymmetric with D5's opt-in (which governs where tenant data physically goes).
8. **KAR-05 broadcast gates = IN** (workstream GATE) — social/ad channels become real Karuna gateways through SIG with consent/DNC, closing an ungoverned outbound path in shipped code (`social_connection_service.py` sits outside SIG/Karuna/consent).

**Increment 6 workstream designs — ✅ ALL SIX WRITTEN (2026-07-25).** [01_learn](./increment-6/01_learn.md) · [02_sega](./increment-6/02_sega.md) · [03_twin](./increment-6/03_twin.md) · [04_strat](./increment-6/04_strat.md) · [05_gate](./increment-6/05_gate.md) · [06_lib](./increment-6/06_lib.md). Each carries a self-contained design, code mapping, task plan and an answered decisions block. **Build not started.** What the design round changed or surfaced:

* **Two live defects in shipped code**, each now its workstream's *first* task rather than a new finding. **(a)** `GET /api/v1/ai/tool-registry` and `.../{tool_id}` are unscoped by company — any authenticated user lists and reads **every tenant's** custom and synthesized tool entries including the `configuration` blob that carries each tool's spec/source/audit. Writes are `app_admin_only`, so this is cross-tenant *disclosure*, not mutation; same shape and same fix as the VG-05 IDOR (SEGA T0, [02](./increment-6/02_sega.md) §2.1). **(b)** the **64 social/ad tools** in `ai/tools/social/` (16 platforms) appear in **no** `TOOL_CATEGORY_MAP` entry, so the PolicyGate `PASS`es every public post and every ad-budget commitment at every autonomy band — an A1 agent, the band where every categorised external effect raises a card, can publish to the internet without one (GATE T1–T2, [05](./increment-6/05_gate.md) §1).
* **B10 resolved as three findings, not one** — cross-tenant policy, reward hacking, drift. The pooled record is a table with **no `company_id` column and no free-text/JSON column at all**, aggregated by a daily job that applies a k-anonymity floor where the company ids are still visible ([01](./increment-6/01_learn.md) §4). Charter decisions 2 and 8 stand or fall together, and LEARN T1 is written as that stop-or-go.
* **The twin plane is a sibling schema (`t_<hex>_tw`), not a third data-plane backend** ([03](./increment-6/03_twin.md) §4.1) — both shipped backends host it unchanged, which retires most of the overview's TWIN risk row.
* **Strategy lives in the tenant data plane** as seven new HBS Planning objects (spine **27→34**, no migration — `seed_hbs_spine` is additive), inheriting the record service, governance, schema evolution, SoR and the Vihara sheet renderer ([04](./increment-6/04_strat.md) §2). **STRAT T2 is a blocking owner review** of the object sheets, like the HBS spine and the Pragya stage scripts.
* **Migrations as designed:** `learn001` → `sega001` → `twin001` → `gate001` → `lib001` (STRAT adds none). **Five** new `ai/` packages, not four: `learning`, `evolution`, `twin`, `strategy`, `library`.
* **KPI history cadence decided (Rahul, 2026-07-25):** daily snapshot for **every non-deleted company**, unmeasurable KPIs recorded as absence rather than skipped, 400-day retention with a reaper.

**Increment 6 / LEARN — ✅ BUILT (2026-07-25, branch `inc6/learn`), closes B10 + VG-12.** All of T1–T10. The `ai/learning/` package (models, pooling, KPI snapshots, outcomes, tuning, harvest, drift, preferences, observation prior, five arq crons), migration **`learn001`** (new head, off `fleet001`), `GET /ai/kpi/history`, `/ai/learning/preferences`, four `learning.*` signal types, and `DemotionTrigger.BEHAVIOUR_DRIFT` in C4. Build notes + eleven deltas: [01_learn.md](./increment-6/01_learn.md) §14. Headlines:

* **B10's cross-tenant half is structural, and it held.** `platform_observations` has **no `company_id`, no free-text and no JSON column** — tenant content is a type error, not a policy violation — and the k-anonymity floor (3 distinct companies) is applied *inside* the aggregation job, where company ids are still visible, writing rows that cannot hold them. Below-floor groups are **dropped, not deferred**. Mutation-tested: adding `company_id` + a JSONB failed exactly the four guarantee tests.
* **Reward hacking answered structurally.** A test greps all of `src/ai` and finds **no KPI key outside `kpi/` and `learning/`** — KPIs are reported, never targeted. Governance-shaped fields are refused at *proposal* time (a refusal that cannot be worn down), and the LEARN⇸SEGA import boundary is a build failure, not a convention.
* **Two design corrections.** The outcome runner is a **sweep, not a completion hook** — `csat_score` arrives *after* a run ends, so a completion hook would miss the best evidence the platform has. And the pooled grain needed a **`coalesce` expression index**, because Postgres treats NULLs as distinct in a unique constraint and the daily upsert would otherwise have appended duplicate "no model" buckets forever (found by the DB-level test; the metadata test could not have).
* **Non-inferiority held on the routing path.** T9's observation correction defaults to no-correction everywhere, and **parity/eval stayed 16 green**.
* **Disclosure updated** ([03a §6](./increment-5/03a_data_flow_disclosure.md), version bumped to `2026-07-25` after verifying existing opt-ins survive it).

**Honest limits:** five crons are wired and **none has run on a live schedule**; charter-tuning proposals reach the bus but nothing consumes them until SEGA exists; `consent_refusal_rate` is unmeasurable until GATE; the pooled store is empty so T9 corrects nothing yet; and the KPI series starts the day this deploys — no backfill, by construction.

**Increment 6 / SEGA — ✅ BUILT (2026-07-25, branch `inc6/sega`), closes B11 + D3 + VG-10 + VG-17.** All of T0–T8. `ai/evolution/` (blast-radius predicate, version ledger, entity canary, staged-rollout sweep, taint ladder + firewall, proposal consumer, schema proposals), migrations **`sega001`** + **`sega002`** (new head). Build notes + eight deltas: [02_sega.md](./increment-6/02_sega.md) §13. Headlines:

* **The tool-registry disclosure is fixed (T0).** Both read paths now scope by company (platform rows stay shared, `app_admin` keeps the fleet view), 404 on a cross-tenant miss, **mutation-tested in two layers**.
* **B11 is closed *end-to-end*, which is the part that matters.** Five limits, each mutation-tested alone — and the predicate is load-bearing at its only caller: bypassing `admit_change` in `evolution/proposals.py` fails three end-to-end refusal tests (governance field, suspended tenant, daily cap). A guarantee nothing routes through is not a guarantee. Rollback is exempt from the rate cap and kill switch but **never** from scope.
* **D3 is closed.** A four-level taint ladder that only descends, a pure total firewall table replacing Increment 1's special case *inside* `evaluate_policy`, resolved from the durable tool log so it survives a paused-and-resumed run. **Non-inferiority proven by mutation: disabling the firewall fails six pre-existing tests.**
* **Two design corrections, both found by building.** T3's stable-hash **traffic split was deleted** — an entity has one row, so an applied change is live for everyone and the split would have compared a version against itself; what ships is an honest staged rollout with a watched window, and `in_canary_cohort` was removed rather than left as dead code with a promise. And **T8 needs no DDL**: tenant object fields are a JSONB list, so `_sync_columns` (the Inc-4 SoR mirror primitive) was never the right host.
* **A defect the tests found:** `record_version` bumped from the entity row, which can lag the ledger, and the resulting unique violation surfaced at the *caller's commit* — so the module's "never loses an edit" promise did not hold. The ledger now decides the number, ordered numerically.

**Honest limits:** the rollout is not an A/B (a true one needs version-snapshot composition in `agent_loop.py`, at its line cap); only signal-driven runs are version-stamped; taint descends from **three of six** entry points (the rest need LIB provenance, record-origin and delegation propagation); `consume_proposals` has no cron yet; and T8 has no *producer* — nothing yet detects that an agent keeps needing a missing field.

**⇒ With B11 and D3 closed, every High-severity finding in the gap register is now closed.**

**Increment 6 / GATE — ✅ BUILT & merged 2026-07-25 (branch `inc6/gate`), closes VG-15.** All of T1–T7, migration **`gate001`**. Build notes + six design deltas: [05_gate.md](./increment-6/05_gate.md) §11. Headlines:

* **The second live shipped-code defect is closed.** `ai/tools/social/` ships **64 tools across 16 platforms** and not one appeared in `TOOL_CATEGORY_MAP`, so the PolicyGate `PASS`ed every public post and every ad-budget commitment at **every** autonomy band — including A1, where every categorised external effect is supposed to raise a card. Two categories now: `broadcast` (shaped like `email_dispatch`) and `ad_spend` (an amount band, plus `HIGH_IMPACT_CATEGORIES` so a hostile inbound message cannot drive money into an ad platform).
* **Mappings are exact, not pattern-matched**, because `youtube_ads_manage_ad_groups` writes while `google_ads_get_ad_groups` reads and one careless needle resolves the wrong one. Exactness costs completeness, so **the totality test supplies it**: every tool under `ai/tools/social/` must map or match a read pattern, and a new uncategorised one fails CI. That is the load-bearing test of the workstream and it is mutation-proven.
* **Two checkpoints, not one** (design said one). Borrowing `before_outbound_payout_above_band` for ad spend would have made an ad campaign **un-opt-out-able** (payout is `platform_mandatory`), AUTO_DENY in 4h rather than 8h, and mislabelled to the approver. Seed **19 → 21**.
* **A third latent defect found in passing:** `evolution/taint_firewall.py` kept a **hand-copied `HIGH_IMPACT_CATEGORIES`** while its docstring claimed it imported one. It drifted the moment the constant moved — `ad_spend` landed in governance and the firewall silently kept permitting it. Now read live, pinned by a test. Any future addition would have half-landed the same way.
* T3/T4 add the tenant's own posture — a channel-and-purpose switch reusing the consent registry (**no migration**) and audience DNC filtering that **filters and counts rather than refusing** — enforced in `SocialMediaTool.run_with_context`, the one method all 64 tools funnel through, so a platform module added later inherits the check. T5–T7 put the channels on the bus: **KAR-05** (roster **18 → 19**, holding neither ad tools nor publish tools by construction), the `broadcast.inbound` producer, and a `broadcast.published` audit that is deliberately **not** subscription-gated.

**Honest limits:** no live platform call (decision 5 — transport injected); `emit_broadcast_inbound` has **no scheduled poller**, so the inbound half only flows when something calls it (the outbound audit *is* live); credentials stay in `social_connections` (decision 4), so two credential stores still exist; and categorising 64 tools **is a behaviour change** — an A1 tenant whose agent has been posting freely will start seeing cards, which belongs in release notes rather than a silent deploy.

**Increment 6 / LIB — ✅ COMPLETE, merged 2026-07-26 (T1+T2 on `inc6/lib`, T3–T8 on `inc6/lib-rest`), closes VG-13 + VG-14 + VG-16.** Migrations **`lib001`** + **`lib002`** (new head, off `twin001`). T8 was kept rather than cut, at the owner's direction. Build notes: [06_lib.md](./increment-6/06_lib.md) §13 (T1+T2, four deltas) and **§14** (T3–T8, six deltas). Headlines from the second half:

* **The rollup needed a third counter the design does not have, because the design's own headline sentence is not what its counters measure.** §5.3 says the rollup answers *"this pricing sheet answered 40 customer questions this month"*; `retrievals` counts **rows**, and one question routinely returns several chunks of one document — so the overstatement is proportional to how finely the *chunker* split it, a property of RETR's configuration rather than of the document. `distinct_queries` is the counter that sentence describes, and it ships beside the other two rather than replacing them.
* **The reaper cannot outrun the rollup, and the clamp is the load-bearing line.** Independent clocks meet in the wrong order eventually: worker down a fortnight, rollup covers yesterday, reaper deletes a fortnight nothing aggregated — both jobs doing exactly what they were told, and a hole in the history nothing reports. `reap_usage_log` clamps its cutoff to the day after the last rolled-up day and **refuses entirely** when nothing has been rolled up. The consequence is deliberate: if the rollup stops, the log grows. Visible and recoverable beats silent loss.
* **Contradiction had to become sticky, and the design never says where it lives.** LIB owns the state and not the detector (§6.1), so `contradicted` has nowhere else to persist — and an age rule able to overwrite it would have made every nightly sweep silently resolve every contradiction nobody had got to yet. The ladder now reads the current state; `raise_contradiction`/`clear_contradiction` are the API the critics call.
* **A defect the tests found: a file that vanished from a connected drive and came back stayed flagged `superseded` forever.** Unchanged content, so the hash comparison skipped it — present in the drive, invisible in the Library, nothing anywhere reporting the inconsistency, and every other drive-sync test passing. Fixed by reviving **in place**: the document keeps its id and with it every usage row and rolled-up day of influence pointing at it. Re-creating would have orphaned the whole influence record of a document whose only offence was being briefly invisible. **Third time this increment a "nothing happened" path was the broken one** (LIB T2's silent `[]`; GATE's silently-permitting taint firewall; this).
* Retrieval now also projects `staleness_state`, `chunk_index`, `filename` and `heading_path` — the same one-column argument T2's `document_id` made, off joins that were already there. Without the first, T4's sweep is invisible at the only moment it matters; without the second, a citation has nothing to open at.

**Honest limits:** no live SharePoint/Google Drive call (transport injected — the Inc-4/Inc-5 precedent); `document_sync` has **no scheduled poller**, exactly GATE's `emit_broadcast_inbound` limit; **nothing calls `raise_contradiction`** yet (LIB deliberately does not own the detector); nothing files artifacts automatically (a producer would put an embedding call inside every tool that writes a file — a cost decision, not plumbing); and **`credentials_expire_at` is never populated**, so T8's sweep is correct and today always empty.

* **T1** — ten provenance columns on `documents`, which had eight columns total and could answer none of spec §15.4's four questions. `effective_from` earns its place: a price list uploaded today may describe last year, and staleness computed from `created_at` would call it fresh. Existing rows backfill to `source_kind='upload'`, everything else NULL — honest, and the reason SEGA's taint ladder reads absent provenance as `external_verified`.
* **T2** — `retrieval_usages`, written **at the caller after rerank**. Placement is not tidiness: logging inside `hybrid_search` records what the *retriever surfaced*; logging here records what the *agent received*. They differ by the whole of the reranker and only the second is influence. The query **text** is never stored (the hash is de-duplication, not an anonymity claim).
* **The mutation test found a real defect rather than confirming a guarantee.** Making the log raise showed `search_semantic` returning `[]` through its catch-all — so a broken analytics feature would have surfaced not as an error but as *agents silently retrieving nothing*. Every unit test passed and the logger's own `try` looked sufficient. The call site now guards independently: the answer path must not depend on the analytics path's discipline.
* Retrieval now carries `document_id` (one column on two SELECTs that already `JOIN documents`) — without it the log attributes nothing and T6's citations have nothing to open.

**Increment 6 / TWIN — ✅ BUILT & merged 2026-07-25 (branch `inc6/twin`), closes VG-09.** All of T1–T11, migration **`twin001`** (new head). New package `ai/twin/`. Build notes + six design deltas: [03_twin.md](./increment-6/03_twin.md) §14. Headlines:

* **The prototype held**, which was the point of doing T1 first. The twin plane is a **sibling schema**, not a third backend: `t_<hex>_tw` on the schema backend, a named `twin` schema inside the tenant's own DB on the container backend. `Plane.LIVE | Plane.TWIN` threads through `schema_name_for` / `ensure_ready` / `session` / `get_tenant_session`, defaulting to LIVE so all 46 existing call sites are untouched. **No migration for the plane** — tenant tables bootstrap per tenant. The overview's "TWIN has no precedent in the codebase" risk is closed.
* **Isolation is proven through the real `RecordService`**, not the plumbing alone: a session carries one translate map fixed at construction, so a twin write has no path to the live schema. Isolated plumbing under a service that resolved its own schema would be no guarantee at all.
* **Honesty grading is computed and never supplied.** No function in `grading.py` and no Pydantic model in `api.py` accepts a `grade` or an `override` — asserted by reflection, so adding one later fails a test rather than quietly softening L6. A mixed result takes the **worst** grade of its inputs (SEGA's monotone taint rule), and `replay` explicitly denies determinism in three places.
* **Substitution is deny-by-default**, stronger than the design's allow-list. Forgetting to allow-list something costs a stubbed call in a rehearsal; forgetting to deny-list something costs **a real external effect from a simulation**. The stub holds no reference to the tool it replaces, and a totality test over `TOOL_CATEGORY_MAP` means a category added later is substituted the day it lands.
* **Twin spend stays TENANT-initiated** (charter decision 7, overriding ratified spec §12.1). `tests/parity` — the attribution canary — stayed 16 green.
* **The delta worth reading: seasonal-naive needed a drift term**, and that was a *correctness* fix rather than a refinement. Plain seasonal-naive repeats last week's value, so a business growing 10% a week would be forecast flat forever. Caught by a test asserting a rising line projects upward.

**Honest limits** ([03](./increment-6/03_twin.md) §14.7): **no scenario runner is wired end-to-end** — T5 selects signals and counts what a handler did, but the handler (an agent loop bound to a twin session + the substituted registry) is not built, so nothing yet writes a `TwinRun` from a real replay; the pieces are each tested, the assembly is not. Also: **T2 materialisation is schema-backend only** (the container backend's live plane is unnamed and needs a different statement shape — it refuses loudly rather than copying from the wrong place); cost rates are **declared, not measured**, and the estimate text says so; the forecast is empty at launch by construction; and wallet holds are not yet drawn because nothing runs a scenario yet.

**Increment 6 / STRAT — ✅ COMPLETE, merged 2026-07-26 (branches `inc6/strat` + `inc6/strat-build`), closes VG-11.** T1–T9. New package `ai/strategy/`; **no migration** — the largest tenant-schema change since Increment 1 (spine **27 → 35**) is seed data. The eight object sheets were drafted, **reviewed by Rahul** ([04a](./increment-6/04a_planning_object_sheets.md), eleven questions, ten answered as recommended) and then built. Build notes + five deltas: [04_strat.md](./increment-6/04_strat.md) §13. Headlines:

* **The review earned its place.** Drafting the sheets against the shipped spine — rather than from the design doc alone — found a genuine arithmetic error ("seven objects, 27 → 34" while listing **eight**; 27 + 8 = 35), a **third** spine-assert site the bookkeeping missed (`test_tenant_records_db.py`'s export-bundle count, which would have failed the build), and three field names that were engineer-speak or spec-speak on a form a business owner reads: `owner_ref` → `owner`, `case` → `rationale`, and `district` → **`concerns_module`** (district is Vihara/Atlas *map* vocabulary).
* **Enforcement needed a seam the record service did not have.** `RecordService` answers *who may write* and *is the shape valid*; neither answers *is this change a legal move*. `tenant_schema/write_policy.py` is a new pluggable seam in the shape KAR used for consent and Inc-4 for SoR — **policy in `ai/strategy/`, enforcement at the existing call site**. It also gave the transition tables a caller, without which they would have been a table rather than a guarantee (SEGA's B11 lesson). Mutation-tested: uninstalling the policy lets `draft → adopted` through.
* **A grade must have a run behind it.** TWIN computes honesty grades and refuses to accept one, asserted there by reflection. STRAT puts a grade on a tenant record a human can edit, so a non-`untested` grade is **refused without a `twin_run_id`** — otherwise typing `replay` into a proposition that never went near the Glasshouse would launder an untested bet into the strongest grade the system has. Planning needs a fourth value TWIN does not have: `untested` (never tried) must not render the same as `unknown` (tried, ungradable).
* **LEARN's reward-hacking guarantee fired on STRAT and was right to.** `test_no_kpi_key_appears_outside_the_registry_and_learning` greps all of `src/ai` for any C6 key. Two files tripped it and in both the key was in an **explanatory comment**, never in code — which only ever reads `target["kpi_key"]` at runtime. The comments were rewritten rather than widening the test: STRAT is precisely where a KPI legitimately becomes a target, so an exemption *there* would be the one most likely to hide a real violation later.
* **The review sweep had to be ORM, not `text()`.** Tenant routing is a `schema_translate_map`, so raw SQL names a schema that does not exist — which is why no raw tenant-plane SQL exists anywhere in `src/ai/`. Recorded in the module docstring.

**Honest limits:** **nothing produces Planning records yet** — the objects seed, the rules hold, the API works, but no agent, Pragya stage or wizard step creates a Minutes or a Proposition; those surfaces are Vihara's Boardroom (Inc 7). `strategy.review_due` is emitted and **nothing consumes it**, so the tray is a poll. Reviews will mostly say `not_measurable` for a quarter because `kpi_snapshots` started 2026-07-25 with no backfill. The TWIN hookup is a **reader, not a runner** — nothing yet takes a Proposition *to* the Glasshouse, and TWIN's own unwired scenario runner is why. And the agents-may-not-adopt rule is **currently unreachable in production** (the ownership gate proposes first, since the Planning owner process has no seeded agent), so it is tested by simulating the future where one exists.

**⇒ INCREMENT 6 IS COMPLETE.** All six workstreams built and merged. Nothing in the increment remains.

---

### Increment 7 / Voice go-live (VG-08) — ✅ BUILT 2026-07-28, awaiting a live call

Five commits (`78a12e4`…`5321e72`). Plan and resume point: [increment-7/00a_voice_go_live_plan.md](./increment-7/00a_voice_go_live_plan.md) §8. Headlines:

* **The scope correction is the thing to read first.** Business voice was *already live* — real Twilio/Tata media through `websocket_handler.py`. VG-08 is **Pragya's inward face alone**: the number an owner dials to reach their account manager. Nobody resuming this should go looking for a broken telephony stack.
* **Two gaps were found that were not in the original four.** **Gap 5:** no Pragya entity has ever existed. Every resolver was written as *"the tenant's Pragya entity, if one is seeded"* and nothing ever seeded one — unnoticed from Increment 3 until a phone number needed somewhere to point (`ai/pragya/seed.py`). **Gap 6:** barge-in was silently dead. `drive_call` set `state.interrupted` from a *non-final* transcript, and Chirp 3 emits **no interim results**, so Pragya would have talked over the caller on every long reply with nothing anywhere reporting it. Replaced by energy VAD in the stream handler, in the receive loop — during synthesis the transcriber is not pulling from the inbound queue, so a check further down would not see the interrupting audio until she had finished speaking.
* **Gap 4 was the one to fix first** and the reason is worth keeping: `route_for_number` resolved the face, **logged it, and fell through to the gateway path anyway** — so gaps 1–3 could all have been completed and a call to Pragya's number would still have landed on the legacy speech-to-speech stack, with nothing obviously wrong in the logs.
* **A test was passing for the wrong reason.** `test_voice_is_refused_when_the_speech_skus_are_unconfigured` asserted the *absence of global configuration* rather than a property of the tenant, so it broke the day voice was configured — the day the feature started working, which is the worst possible moment for a test to fail.
* **No test may reach a live transport** (§3.2 there). The moment the transports stopped raising `NotImplementedError`, the two "not built yet" tests started opening real gRPC channels and failing on ADC reauth. A suite that needs credentials to pass is a suite that stops being run.
* Two further owner decisions: **one shared number for every tenant** (inverting decision 5, with the security consequence stated) and **one address belongs to at most one tenant**, enforced by a partial unique index (`iauth002`) rather than resolved at call time — a wrong pick there is a cross-tenant disclosure read aloud over the phone.

**Honest limit: no line of either live transport has ever executed.** They are written from SDK contracts, not from a successful call. The first run is the test.

### Increment 7 (Vihara) — ✅ PHASE A EXITED 2026-07-29 (R1 + R2 passed); Phase B decomposed

**Eight decisions locked** ([increment-7/00_charter.md](./increment-7/00_charter.md) §Decisions — do not re-litigate): Phase A next · **the Buddha Cognitive Lab design system governs all three renderers** · day–night survives as luminance · `vihara/` is a new top-level app in this repo with auth and API client rebuilt · stack as spec §12 recommends · the Line is an installable **PWA** · push is self-hosted **Web Push/VAPID** · portrait direction written now, rasters before G1. Plus the sequencing: **all Phase-A deliverables before any G0 code**, stricter than spec §13 permits, by owner choice.

**All eight deliverables written** ([01_phase_a_overview.md](./increment-7/01_phase_a_overview.md) §8 is the summary): [02 stack](./increment-7/02_stack_and_repo.md) · [03 art bible](./increment-7/03_art_bible.md) 🔍R1 · [04 component registry](./increment-7/04_component_registry.md) · [05 manifest contract](./increment-7/05_manifest_contract.md) · [06 backend API contracts](./increment-7/06_backend_api_contracts.md) · [07 wireframes](./increment-7/07_surface_wireframes.md) 🔍R2 · [08 device matrix](./increment-7/08_device_matrix.md) · [09 parity register](./increment-7/09_screen_parity_register.md). Two viewable specimens: `increment-7/art-bible/specimens.html` and `increment-7/wireframes/spine.html`.

What the phase decided that the spec did not:

* **The gold budget.** Under a single-accent brand, gold was being asked to mean seven things at once. It now means exactly two — *this needs you*, or *this is certified* — which is what makes a still estate **visibly** still: at zero hands raised there is no gold on the screen at all.
* **The certified set is derived, not chosen.** A component is certified iff it drives a backend endpoint calling `enforce_tier`/`enforce_kind`. That turns spec §9.2's six into **ten**, and gives a CI correspondence test spanning the repo boundary. It survived the wireframe pass unchanged at ten — which is its correctness proof, since a certified component discovered by drawing would have meant an act worth a ceremony that the backend does not gate.
* **The Glasshouse is solved by desaturation, not a second hue** — no new colour, the right metaphor, and unforgeable because the renderer applies it at the plane boundary.
* **`honesty_grade` has four values.** `untested` (never tried) must not render like `unknown` (tried, ungradable), and a grade asserting a simulation requires a `twin_run_id` — STRAT's record-layer refusal carried into the layer a human reads.
* **There is no `ui_manifests` table**, despite technical §8 and VG-01 both sketching one. What audit actually needs is a hash on the approval record.
* **Weather is projected on read, never stored** — a stored state can disagree with the numbers it describes.
* **The shell is app-owned, not manifest-composed** — a hostile manifest cannot remove the user's way out of a surface.
* **A tier-C device never downloads three.js** — a hard build gate, because one careless static import undoes it invisibly.

**Three new findings.** **VP-01** (`localStorage` tokens in an app that renders generated UI and drives step-up) — **resolved**: access token in memory, refresh token in an `HttpOnly` cookie, legacy untouched, ~half a day of backend work. **VP-02** — the procedural seal portrait direction has no production cost and cannot drift; owner call at R1. **VP-03** — **three tenant functions have no Vihara surface at all**: pre-session, account & security, billing & wallet. Passkey enrolment is inside that gap and is the prerequisite for every T2 act, while the spec's only candidate home is depth 3, which is operator-density and desktop-only.

**Four corrections to shipped documents:** the tier gate has **six** certified endpoints, not five (STRAT added resolution adoption 2026-07-26 and no document caught it); **VG-21 was already closed** by LEARN and sat marked open at severity M; **"59 screens" is a file count** — five are not screens, so the denominator is 54 and cutover parity is **28 of 30 in-scope tenant screens**; and **spec §11 is superseded**, marked in place with what is retired *and* what is carried forward both named.

**Increment 6's six workstreams**, build order forced (see [00_overview.md](./increment-6/00_overview.md) §3): **LEARN → SEGA → TWIN → STRAT**, with **GATE** and **LIB** parallelisable throughout. SEGA before TWIN because TWIN's promotion pipeline *calls* SEGA's entity-change canary; TWIN before STRAT because STRAT's reviews read TWIN's honesty grades; LEARN first because `kpi_snapshots` is the longest-lead time series in the increment and four later surfaces read it. **Most of this increment is stores, and stores are worthless empty** — a KPI history started in Inc 7 cannot answer "was Week 12 better than Week 1" until Week 12 of Inc 7.

**VG-05 / VR-12 — ✅ BUILT 2026-07-24 (the first Inc-6 code).** `ai/inward_auth/guard.py` closes the REST-path tier gate: five certified endpoints (approval respond · connector bind · master apply · provider opt-in · autonomy raise) now classify through the *same* `classify` + `require_tier` Pragya uses. **A cross-tenant IDOR was found and fixed while mapping the work** — `respond_to_approval` selected the approval **by id alone** while `get_pending_approvals` right above it had always joined on company, so any authenticated user with an approval UUID could approve another tenant's HITL card. Also: `GateDecision.amount` now reaches `context_snapshot` (without it every high-impact approval failed up to T3 by *artifact*, not policy) and `IntentKind.CONNECTOR_BINDING` was added across classifier + Pragya schema + keyword screen. Gates: typecheck 260 · **1550 unit** · 16 parity/eval · **288 integration**. Three controls **mutation-tested**. Build notes + four design deltas: [00a](./increment-6/00a_genui_backend_gap_analysis.md) §7. ~~Honest limit: the React app does not yet render the step-up refusal~~ — **closed 2026-07-25** (below).

**VG-05's frontend half — ✅ BUILT & merged 2026-07-25** (`merge(inc6): console step-up refusal handling`). `parseStepUpRefusal` in `authn.service.ts` claims only a 403 carrying the `step_up_required` marker, so an ordinary permission denial still reaches the caller's own error path; the `useCertifiedAction` hook runs the caller's action as a **closure and retries it whole** after the ceremony (no second success path to keep in step), retrying exactly once. Wired into `HITLPanel` (approval respond) and `EntityBuilder` (autonomy raise) — the other three gated endpoints have no console surface yet. Backend delta: `tier_refusal` now carries **`command_ref` + `command_summary`**, without which a **T3** refusal is uncompletable (the out-of-band leg binds its nonce to a command, and one the client invented would let a confirmation for one command authorise another); `approval_summary` states what a HITL card authorises, read off the gate's own snapshot. Gates: typecheck 260 · **1554 unit** (+4) · `npm run build` clean · integration certified-actions + connectors-router 13. **Honest limit: the frontend has no test runner configured** (no vitest dependency, no `test` script), so the FE half rests on the build gate; the refusal *shape* it depends on is pinned by backend tests.

**Closed by PRAGYA-RT:** the four Inc-3 build gaps (stage advancement + artifact extraction T3, deferred reflection T6, script goldens T7), plus the two audit defects and Pragya's tool surface (T9, child entities). The Inc-3 deferred-set *correction* (Strategize/Decide are `SKIPPED`, not deferred) is what made T6's queue drainable.

The push in §1 is **done** — there is no standing external action left.

**Done 2026-07-22** — TRUST completed ([05](./increment-2/05_trust_billing_safety.md) §15–§18): **E4** fee-formula guard, **E2** free-credit abuse controls, **E1** idle-cost model ([05a](./increment-2/05a_idle_cost_model.md)), and both open integration hookups (C5's `days_past_due` driver, B13's admission at the dreaming runner). RETR completed ([06](./increment-2/06_retrieval_upgrade.md) §5–§9): all five tasks, MRR 0.812 → 1.000 over pure cosine on the golden set.

**Done 2026-07-20** — PACK ([03](./increment-2/03_pack_agents_processes.md) §8): full Wave-0 roster + runtime SoD. KAR ([02](./increment-2/02_kar_gateways.md) §5): WhatsApp via SIG + gateways + injection golden + consent seam. ONBOARD backend ([04](./increment-2/04_onboard_wizard.md) §6): the `/ai/onboarding/*` wizard step APIs over `activate_bundle` + the `/ai/loop/envelope` admin GET. **The remaining ONBOARD frontend (React wizard, approvals console, FE gates) is a separate FE track** — the backend contract it calls is done.

Each workstream doc has: self-contained design, code mapping, a task plan, and a **§ Brainstorm Decisions** block (already answered — do not re-litigate). Follow the Increment-1 rhythm: branch `inc2/<workstream>`, build task-by-task, keep gates green, add a §N build-note delta log + flip maturity tags on merge.

**Voice (KAR-01 real) + B7** are **deferred to Increment 3** (with Pragya, who needs the same realtime path).

---

## 4. Locked decisions (Increment 2 — do not re-open)

From `increment-2/00_overview.md` §2 and each doc's decisions block:

1. **Thin vertical slice first** (done: email→P03).
2. **Async-first MVP; voice deferred** to Inc 3. KAR-01 is a registered stub.
3. **Curated hand-authored agent templates** (reviewed like the HBS spine); the Meta-Agent Board is for tenant-*custom* agents.
4. **Wizard onboarding; Pragya in Inc 3** — the wizard's step APIs are authored as Pragya's stage APIs.
5. **Global-neutral market** — pluggable consent/DNC + multi-currency; no hard-coded jurisdiction.
6. **Graduated dunning** — notify → grace (7d, configurable) → **read-only** (agents stop acting, inbound parked-not-dropped) → suspend.
7. **All bundles included at all subscription tiers** (no per-tier bundle gating).
8. **Consent is tenant-configured from day one** (no imposed global opt-in default).
9. First slice = **email → P03 acquisition**.

---

## 5. Conventions & gotchas (learned this session — save future debugging)

* **New `ai/` package → add it to the strict-typing allowlist** in `backend/scripts/typecheck_ai.py` (`CLEAN_PACKAGES`). Current: governance, orm, planning, meta, memory, core, signals, tenant_schema, loop, solo_pack.
* **`backend/src/ai/core/agent_loop.py` is pinned at its 1500-line cap** (layout lint). Do NOT add code there — extract to a helper module (that's why `gate_and_maybe_stop` lives in `policy_gate.py` and wallet-hold admission lives in `arq_jobs.py`).
* **De-canary layout lint** bans the token `P11`/`p11`/`phase11` in `ai/` source (it flags leftover phase-11 canaries). The HBS fixture stores owner codes numerically (`"owner": 11`) and renders `P{n:02d}` at seed for exactly this reason. Don't write a literal `"P11"` in `ai/` source or comments.
* **Tenant schema routing** uses SQLAlchemy `schema_translate_map` (symbolic `tenant` schema → `t_<hex>`), NOT `SET search_path` (which leaks onto pooled connections). Tenant tables use a separate `TenantBase`.
* **Records are tenant-DB; signals/runs/billing/memory are control-plane.** Cross-DB emits (e.g. `object.change_proposed`) go through a *separate* `AsyncSessionLocal` transaction — never assume one transaction spans both planes.
* **Wallet-hold admission reads the balance from the FOR-UPDATE-locked row directly** (`_locked_available`), never via `CreditService.get_balance` — that method's daily-credit auto-injection commits and would release the lock mid-admission (re-opening the E3 race).
* **Solo Pack tools register at the entry points** (`worker.py` + `main.py` via `register_solo_pack_tools()`), NOT from `tools/__init__` — importing `solo_pack.tools` from that package init cycles.
* **Integration tests dispose the global engine** at fixture setup (`await engine.dispose()`) — pytest-asyncio gives each test its own loop and asyncpg can't share pooled connections across loops. Follow the self-managed committed-fixture pattern (create company + tenant schema + wallet, clean up in `finally`, delete child tables before parents: signals before execution_runs, etc.).
* **PolicyGate only gates *categorised* acts** (`CATEGORY_RULES` + `TOOL_CATEGORY_MAP` in `governance/authority.py`). A new money/comms tool needs a category + mapping or it won't be governed. At A1 any categorised external effect → HITL; at A2+ it's band-based/autonomous.
* **Testing LLM-driven agents:** don't script the prompt-hash-keyed MockLLM through multi-run flows (fragile). Drive the *real platform seams* deterministically (signal dispatch, record graph, PolicyGate) and supply each agent's tool-calls as the unit of work; leave prose quality to eval goldens. See `test_solo_pack_slice_e2e.py`.
* **Solo Pack tree lives in a manifest, not in each template** (PACK): `templates.PROCESS_GROUPS` (`ProcessGroup(process, agents)`) + `GATEWAYS` encode parentage; `activation._activate(groups, gateways)` is the one seeding core. Adding an agent = add it to its `ProcessGroup`; adding a process = add a `ProcessGroup`; a new gateway (KAR) = append to `GATEWAYS`.
* **Bundle process codes are stored NUMERICALLY** (`bundles.py`: `(8, 9, 10, 11, 18)`) and rendered `P{nn}` on read — the de-canary lint bans the literal `P11` token in `ai/` source and the Fiscal bundle's §2.1 membership includes it. Same trick as the HBS spine's `owner: 11`. Never write `P11`/`p11` in `ai/` source.
* **TRUST's shape: policy in `ai/trust/`, enforcement at the existing call site.** Every TRUST piece follows it — the ladder in `dunning.py` enforced by `common/middleware.py`, the registry in `consent_registry.py` filling KAR's seam, the eligibility predicate in `abuse_controls.py` enforced in `billing/credit_service.py`. It keeps the policy strict-typed and unit-testable without a DB, and touches the legacy call sites minimally. `ai/` → `billing/` imports are long-established (`step_executor`, `loop/wallet_holds`, …); the reverse is fine too.
* **Free daily credits have THREE injection points, not one** — `auth.service._provision_new_tenant` (registration), `credit_service.get_or_create_wallet` (first touch), and `flush_and_inject_daily_credits` (the daily cron). A gate on only the cron is bypassable by signing up and immediately using the product. `_provision_new_tenant` runs **pre-commit and must not query other tables** (asyncpg can't do concurrent ops on one connection), so gate it from a settings flag + the company's own metadata, not a DB lookup.
* **Anything that withholds credits must be scoped to self-service signup** (`onboarding_metadata.created_via`), never to "has no verified user". Fixture/seeded companies have **no users at all**, so the broad form fails every agent run in `tests/parity` with `insufficient_funds` at wallet-hold admission. **The parity suite is the canary for credit-path changes** — `tests/unit` stays green because it doesn't drive real runs, so run `pytest tests/parity tests/eval` before believing a billing change is safe.
* **Retrieval has three stages and they are deliberately separate** (RETR): `hybrid_retrieval.hybrid_search` fuses, the domain viewport filters *inside* it (after fusion, before `top_k`), and `reranker.llm_rerank` runs at the caller. Do not fold the reranker into `hybrid_search` — it would cycle on `RetrievedChunk`, and it would put per-query LLM spend inside a function that other call sites reasonably assume is cheap.
* **A new `usage_logs` attribution must be classified for B13.** Adding a `CostAttribution` member is not enough — decide whether it belongs in `PLATFORM_INITIATED_ATTRIBUTIONS`. Tenant-asked-for work (like RETR's `rerank`) must stay out, or ordinary tenant activity can exhaust the cap that exists to protect tenants *from* platform work.
* **`setuptools` is pinned `<81` on purpose** (AUTH): 81 removed `pkg_resources`, which `opentelemetry-instrumentation` imports at module scope, so an unpinned resolve breaks `import src.main` outright. If a `poetry add` ever unpins it, `import src.main` is the fastest check. More generally: **this VM's venv had drifted from `poetry.lock`** (manually pip-installed `google-genai` 2.x / `httpx` 0.28 over a lock saying 0.4/0.26), so any `poetry add` re-syncs the venv *down* to the lock. Re-run the unit suite after touching deps.
* **Strict typing does not cover SQLAlchemy's `func` namespace** (PRAGYA): `func.case(...)` type-checks fine under mypy `--strict` because `func.*` is `Any`, and then fails at runtime — the correct import is `sqlalchemy.case`. Anything built through `func` needs a test that actually executes the query.
* **Pragya has her own turn loop but not her own substrate** (Inc-4 seam, locked): governance, `inward_auth`, tool execution, billing, CORTEX, the signal bus and the Meta-Agent are 🔒 shared. `ai/pragya/acting.py` is the **only** module that may reach the tool executor — an import-boundary test enforces it, and that guard was verified to fail on an injected violation.
* **A checker never observed to fail is a function that returns `True`** (T7): every behavioural golden has a fixture written to violate it, the mapping is asserted total, and each fixture must break only its own check.
* **An `ai/` package init must not import back toward its own consumers** (VOICE): `voice_loop/__init__` re-exporting `identity` closed the cycle `inward_auth.bindings → solo_pack.consent → templates → voice_loop.profile → identity → bindings`. The init re-exports only `profile`; import submodules directly. Same rule the Solo Pack tools follow.
* **Check a channel ceiling before session state, not after** (VOICE): `voice_tier_ceiling` applies the T1 voice cap first, so an elevation earned in the console cannot ride onto the next phone call.
* **Resolve an ambiguous command narrowly, never broadly** (PRAGYA): an unscoped "pause" asks which process rather than pausing all of them. `commands.ALL_TRIGGERS` is reached only by the kill switch or an owner who literally said "everything". A missing target is a question, not a wildcard.
* **A gate must be reachable by the test harness that claims to cover it** (VG-05): this repo tests routers by **calling handler functions directly**, and a FastAPI `dependencies=[Depends(...)]` entry does *not* run on a direct call. A decorator-shaped security gate is therefore invisible to those tests and can be deleted without failing one. Put the enforcement call **in the handler body** (`inward_auth/guard.enforce_kind`). Same reasoning as the behavioural-goldens rule: a checker never observed to fail is a function that returns `True` — so **mutation-test security controls**, one at a time, and confirm each breaks only its own test.
* **Scope first, then gate** (VG-05): load the object through its company join *before* asking whether the human may act on it, and return **404 (not 403) on a cross-tenant miss** — a probe must not learn that an id exists in another tenant. `AIService.respond_to_approval` shipped for months selecting `HumanApproval` by id alone while `get_pending_approvals` on the adjacent line joined `ExecutionRun` for company scoping. **When adding a write beside an existing read, copy the read's scoping.**
* **`require_tier` now has REST call sites** — `ai/inward_auth/guard.py`. A new certified endpoint (money, credentials, autonomy, consent, bulk) needs an `enforce_tier`/`enforce_kind` call or it is ungoverned on the console path even though Pragya's path is governed. Note **only the raising direction is gated** (autonomy up, consent in) — lowering a band and revoking consent stay open, because the safe direction must never be harder than the unsafe one.
* **AUTH's two gates are different gates.** The PolicyGate asks "may this *agent* do this?" and raises HITL cards; `inward_auth.require_tier` asks "did this *human* prove enough to ask?". They share only `CATEGORY_RULES` — do not merge them, and do not let a Pragya command satisfy its own approval (HITL cards are console-only artifacts by construction).
* **Never elevate inside a verify function** (AUTH): `verify_totp` / `finish_authentication` return a result and the *router* elevates, because a failure must be counted against the lockout counter. A new factor must funnel through `api._fail_step_up` or it silently disables brute-force protection.
* **Cross-process record writes need `actor_process_code`** (PACK SoD): the `tenant_record_write` tool resolves it from `agent_id` (own `process_code`, else parent process's); a write on another owner's object then *proposes* (`object.change_proposed`). A gateway/origination write (no PROCESS ancestor) resolves to `None` → front-door. A new money/record tool should thread `actor_process_code` the same way.
* **A reaper must never be able to outrun the job that aggregates what it deletes** (LIB T3). Two sweeps on independent schedules meet in the wrong order eventually — the worker is down a fortnight, the rollup covers only yesterday, and the reaper deletes a fortnight nothing aggregated. Both jobs did exactly what they were told and the loss is silent. `library/influence.reap_usage_log` clamps its cutoff to the day after the last rolled-up day and **refuses entirely when nothing has been rolled up**. Prefer a visible, recoverable failure (the log grows) over a silent irrecoverable one. Related, and already in this list: put the reaper *in the same job* as the producer it bounds (`kpi_snapshot_sweep`), because a reaper on its own schedule is a reaper that eventually stops being deployed.
* **Suspect the "nothing happened" path — it is where this codebase hides its bugs.** Three times in Increment 6: LIB T2's mutation test found `search_semantic` returning `[]` through a catch-all (a broken analytics feature surfacing as *agents silently retrieving nothing*); GATE found `taint_firewall` silently **permitting** because it held a hand-copied constant; LIB T7 found a returning drive file silently staying `superseded` — present at the source, invisible in the Library. All three read as working from every other angle, and every unit test passed. When a code path's failure mode is "quietly does less", write the test that removes the thing and puts it back.
* **Tenant-plane queries must be ORM, never `text()`** (STRAT T6). Tenant routing is a SQLAlchemy `schema_translate_map` (symbolic `tenant` → `t_<hex>`), and a raw statement bypasses it — the literal schema it names does not exist. This is why there is no raw tenant-plane SQL anywhere in `src/ai/`, a fact that reads like an accident until you try it.
* **A domain rule needs a seam, not a branch in the record service** (STRAT T5). `RecordService` answers *who may write* (owner-writes/others-propose) and *is the shape valid* (`validate_record_data`); neither answers *is this change a legal move*. `tenant_schema/write_policy.py` is the seam — **policy in the owning workstream's package, enforcement at the existing call site**, the same shape KAR used for consent and Inc-4 for SoR write-back. Note it fails **open** on an unexpected error, deliberately: an object rule is a correctness guard over a tenant's own records, not a security control, and none of this codebase's security controls route through it.
* **A gap register goes stale in days, not months** (Inc-7 Phase A). VG-21 was closed by LEARN on 2026-07-25 and was still listed *open at severity M* on 2026-07-28 — nobody was careless, the register simply has no mechanism. Where a finding's closure is checkable by code, **make it a test rather than a note**: the certified-set correspondence rule (a component is certified iff a backend endpoint calls `enforce_tier`/`enforce_kind`) is why the "five certified endpoints" line will not drift again, and STRAT's sixth had already been missed by three documents before it was written.
* **Certification is decided once, in `inward_auth/guard.py`, for every surface** (Inc-7 D3). A new console, app or channel must **derive** its certified set from the `enforce_*` call sites rather than transcribing a list — that module's own docstring makes the point: *"If the console and Pragya ever disagree about a tier, it is a bug in one call site's intent, never in two copies of the rules."*
* **A counter's name must match the sentence it is supposed to answer** (LIB T3). `retrievals` counts rows in the usage log; "this document answered 40 customer questions" counts *questions*, and one question returns several chunks of one document — so the row count overstates influence in proportion to how finely the **chunker** split that document. Before shipping an aggregate, say the sentence it exists for out loud and check the `GROUP BY` computes it.

---

## 6. How to run the gates (from `backend/`)

```bash
cd backend
poetry run pytest tests/unit -q                          # ~950 unit tests
poetry run pytest tests/parity tests/eval -q             # engine parity + eval harness
poetry run python scripts/typecheck_ai.py                # mypy --strict over the allowlist
poetry run python scripts/lint_ai_layout.py              # layout + de-canary lint (exit 0)
poetry run pytest tests/integration/test_*_db.py -q      # needs a live Postgres (DATABASE_URL)
poetry run alembic upgrade head                          # apply migrations
poetry run mypy --strict --follow-imports=silent src/ai/<pkg>   # one package
```
Integration tests **skip cleanly** without `DATABASE_URL`. Docker is available for the tenant-DB container backend (default backend is `schema`, needs no Docker).

---

## 7. Ops remainders (human/prod-only — track separately)

* **`hb-tenant-db` container go-live** (SCH): build `backend/docker/tenant-db/`, `trivy` scan, push to registry, pin the digest, then flip `TENANT_DB_BACKEND=container`. Until then the `schema` backend is the tested default. Mirror the shipped sandbox S7 path.
* **Increment-0 items** were reported done by Rahul (PyPI publish of `hb-cortex-memory`, sandbox canary). Re-confirm before prod GA.
* ~~**Frontend** (ONBOARD, Inc-2): the admin UI + onboarding wizard are not built~~ — **stale, built 2026-07-22** on `inc2/onboard-fe` and merged (§2). The Node toolchain works on this VM (§1 note).
* **Voice go-live is BUILT (2026-07-28) and awaiting a live call** — a **G3 prerequisite** for Vihara, not an ops remainder. Two config items and a phone stand between it and proof: the ASR registry row's region reads `us-central1` and Chirp 3 needs the `us` multi-region, and ADC needs re-authenticating where the worker runs (`gcloud auth application-default login`). Then `poetry run python scripts/setup_pragya_voice.py --check`. 🚧 **Plan and resume point: [increment-7/00a_voice_go_live_plan.md](./increment-7/00a_voice_go_live_plan.md).** Read the scope correction there before starting — **business voice is already live** (real Twilio/Tata media through `websocket_handler.py`); VG-08 is *Pragya's inward face* alone. Four gaps, not the three Inc-4 §12.5 named: `route_for_number` resolves the face, **logs it and falls through**, so the other three can all be finished and a call still lands on the legacy stack. Phases 3–4 need no credentials (injected transport, the FLEET/TWIN/LIB precedent).

---

## 8. Register findings status (from the gap register)

* **Closed by Inc 1:** A6, B1, B2, B3, B5, B6, B8, E3 (+ the earlier v3 doc-pass batch).
* **Closed by Inc 2:** **C1** (all 6 Wave-0 process sheets — P03 in SLICE, the other 5 in PACK) + the Inc-1 owner-id-resolution & governance-band-seeding carryovers (PACK); **D6** (consent registry), **C3** (per-checkpoint HITL SLAs), **B13** (platform-initiated budget class), **C5** (graduated dunning + state-aware suspension), **E1** (idle-cost model), **E2** (free-credit abuse controls), **E4** (fee-formula guard) — all TRUST; **B8**'s retrieval half (RETR). **Increment 2's finding list is fully closed.**
* **Closed by Inc 3:** **D1** (AUTH built the mechanism; PRAGYA wired `require_tier` into `handle_turn` — closed end-to-end), **C4** (autonomy demotion triggers + daily sweep + anti-rubber-stamp promotion evidence), **C6** (KPI registry with the honest-absence rule), **C8**'s per-stage-script half (stages 1–5 reviewed and shipped).
* **Closed by Inc 3 / VOICE:** **B7** (realtime voice vs the loop) — the collapsed-loop profile plus context-preserving live transfer.
* **Inc 4 / CONN+SOR (2026-07-23):** **B4**'s mastering machine **built** (write-back-first, master-wins, sync-in, ownership migration on the Zoho flagship). **D2** and **C2** **deferred** (per-company credentials chosen; human-task surface separable).
* **Closed by Inc 5:** **B12** (model registry — version/region/effective-dated price, reproducible billing — REG) and **D5** (conservative default allow-list + auditable revocable per-tenant opt-in + the published data-flow disclosure — FLEET), plus **B9**'s build side (the §22.2–.4 admission gate + canary — EVX).
* **Closed by Inc 6 (2026-07-25/26) — the increment is complete:** **B10** (cross-tenant learning, structurally — LEARN), **B11** + **D3** (blast-radius limits, taint ladder — SEGA), and the gap-analysis findings **VG-05**/**VG-09**/**VG-10**/**VG-11**/**VG-12**/**VG-13**/**VG-14**/**VG-15**/**VG-16**/**VG-17**. **With B11 and D3 shut, every High-severity register finding is closed.**
* **Increment 7 / Phase A (2026-07-28, exited 2026-07-29):** **VR-10** closed (the parity register); **VG-21** found already closed by LEARN; **VG-08**/**VR-11** built and awaiting a live call; **VG-22** specified but unbuilt. All three Phase-A findings are resolved: **VP-01** (built by SEAM T9), **VP-02** (portraits A with seal fallback, R1), **VP-03** (The Study, R2).
* **Closed by Inc 7 / SEAM (2026-07-29):** **VG-01** (serving half — registry + manifest service), **VG-02** (estate read model), **VG-03** (live stream), **VG-04** (tray composer), **VG-06** (echo bus), **VG-07** (Pragya channel), **VG-19** (push), **VG-23** (the taint rule enforced at compose and on cache hits), and **VP-01**. Phase B's decomposition ([10](./increment-7/10_workstream_decomposition.md) §10) assigns every remaining VG finding a workstream.
* **Closed by Inc 7 / DRIVER (2026-07-29):** **VG-18** (termination as workflow — `ai/talent/`, a plain governed act by owner decision) and D5 §4.1's per-path cost gap (the estimator, observed-median-or-null). **VP-03 is closed at build level** — the Study drafted (D6 §15a) and built.
* **Closed by Inc 7 / STEWARD (2026-07-29):** the three inherited debts earlier build notes named STEWARD's — `deliver_tray` now has its production caller (the approval watcher), the tray `recommendation` its writer (LLM, owner decision), and the tray panel's extra SSE connection its consolidation. The T3 second-channel ceremony runs end to end from Vihara. G3's **exit** keeps the two live legs (the phone call · a live browser mic run).
* **Closed by Inc 7 / LINE (2026-07-29):** **VG-20** (the Private Line backend — smaller than sketched: the story is a shared composition, the thread already persisted, the passkey is the device registration; the one new store is `morning_stories`, and only because of the pre-generated-audio decision) and spec §14.3's read-mirror (trays through the one door's last resort + the daily morning summary). G4's **exit** keeps the real-device legs.
* **Closed by Inc 7 / GLASS (2026-07-29):** **the TWIN scenario-runner assembly** — the honest limit Increment 6 refused to call done — plus the substituted registry's missing consumer, TWIN's undrawn wallet holds, and the unconsumed `twin.promotion_proposed` signal. G5's **exit** keeps the pilot walkthrough. Remaining for later workstreams: **VG-22** (POLISH proves the latency budget).
* **Still open (later increments):** **B14, D4** (**Inc 8** — renumbered 2026-07-24; Vihara took Increment 7), **C2, D2** (deferred within Inc 4). *(Register reconciled 2026-07-23 — C4/C6 were closed by Inc 3, not open; the gap register's inline row Statuses were corrected the same day.)*

---

## 9. One-paragraph orientation for a fresh agent

> The repo is a multi-tenant agentic platform. **Increments 1–5 are built and merged to `master` — and pushed** (verified 2026-07-24: `origin/master` = local `master` = `a403cda`): the one-loop substrate (SIG/GOV/SCH/LOOP+ENV), the sellable Solo Pack MVP (all six Inc-2 workstreams), Pragya v1 + inward auth + realtime voice (Inc 3), the connected business — connectors/SoR on the Zoho flagship plus Pragya's own runtime (Inc 4) — and the Intelligence Engine: model registry, router v1+v2, the EVX admission gate, and GLM/Qwen/Kimi behind the D5 opt-in (Inc 5). The **GenUI Design Gate is passed at design level** (2026-07-24, §3): owner-selected hybrid + the ratified **Vihara** spec ([genui_design_gate_spec.md](./genui_design_gate_spec.md)), and the spec has now been **walked against the code** — [increment-6/00a_genui_backend_gap_analysis.md](./increment-6/00a_genui_backend_gap_analysis.md) records 23 backend + 12 road-map gaps and proposes adding **TWIN** and **STRAT** to Increment 6. **Increment 6 is COMPLETE** (2026-07-26) — nine decisions locked, all six workstream docs written, and **LEARN, SEGA, TWIN, STRAT, GATE and LIB all built and merged**: B10, B11 and D3 closed (so every High-severity register finding is shut) plus gap-analysis VG-09 through VG-17. Migration head **`lib002`**; STRAT added none, because its eight Planning objects are HBS seed data (spine **27 → 35**). Both live shipped-code defects the design round found are fixed. Gates on `master`: typecheck **317** strict · layout lint · **1996** unit+parity/eval · **490** integration. **Nothing in Increment 6 remains.** **Increment 7 is under way.** **Voice go-live (VG-08) is BUILT** (2026-07-28) — six gaps closed, two of them found while building, migration `iauth002` — and **awaiting a live call**, blocked only on two config items and a phone ([increment-7/00a](./increment-7/00a_voice_go_live_plan.md) §8). **Vihara's Phase A is complete and awaiting two owner reviews**: eight decisions locked and eight deliverables written ([increment-7/01](./increment-7/01_phase_a_overview.md)), no code, exiting when **R1** (the art bible) and **R2** (the wireframes) pass. The load-bearing decision is that the **Buddha Cognitive Lab design system**, which landed in the repo that week, now governs all three renderers — **superseding ratified spec §11**.

Four things to know before picking up what is next: **(1)** **the push is BEHIND** (~58 commits: SUB's tail, WORLD, portraits, DRIVER, the vhost deploy, STEWARD, LINE, GLASS) — this VM has no git credentials, so pushing from a credentialed host is the standing owner-side action; **(2)** **Phase A exited; ALL EIGHT workstreams are built** (2026-07-29) — SEAM, SUB, WORLD, DRIVER, STEWARD, LINE, GLASS **and POLISH**; the increment's build phase is complete. POLISH closed **VG-22 at the harness level** (the last open gap-analysis finding — the device leg is a run-sheet row), staged onboarding in the world (depth 0 = stage 9's reward), applied the art bible everywhere (the atmosphere layer, glass, the motion table, the surface idiom — after the owner's review found the screens nowhere near the R2 wireframes), and wrote **[increment-7/15a](./increment-7/15a_launch_protocols.md)** — the one document holding every owner-side leg; owner-side checks outstanding (all consolidated in 15a §3): G1's two items ([08](./increment-7/08_device_matrix.md) §9.3 — the real-device matrix, the walkable look), G2's pilot walkthrough ([11](./increment-7/11_driver.md) §6.3), **G3's two live legs** ([12](./increment-7/12_steward.md) §8 — the phone call, a live browser mic run), and **G4's real-device legs** ([13](./increment-7/13_line.md) §7 — Android + iPhone install, a push as a tray, a fingerprint approving a payment), and **G5's pilot walkthrough** ([14](./increment-7/14_glass.md) §8 — a real scenario replayed against a real tenant's yesterday, promoted through a staged rollout); **(3)** voice go-live's **two config items are done** (ASR `region=us`, ADC working) — the live-call proof remains owner-side; note this VM's DB still shows no shared line assigned and no Pragya entity seeded, so the call evidence lives in production if anywhere; **(4)** several stores are built and **empty by construction**, and several producers deliberately unwired (nothing calls `raise_contradiction`, nothing polls a connected drive; DRIVER's honest limits are in [11](./increment-7/11_driver.md) §6.3, STEWARD's in [12](./increment-7/12_steward.md) §9.3, LINE's in [13](./increment-7/13_line.md) §8.3, GLASS's in [14](./increment-7/14_glass.md) §9.3 — **the scenario runner is now wired**, but no scenario has run against a real tenant's history, the forecast path has no producer, the promotion sweep has no cron, the morning job's first live morning is production's, and speech metering is still VG-08's open debt) — each recorded in its workstream's build notes. Keep every gate green; follow the conventions in §5 above.
